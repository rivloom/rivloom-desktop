# 内测安全边界

更新：2026-09-05，适用于 M3.5 会话式 Node 协作的当前实现。使用入口见 [README](README.md)，随包组件和许可见 [第三方声明](THIRD_PARTY_NOTICES.md)。

此 MVP 适合可信参与者和专用 Windows 测试环境。**不提供操作系统安全沙箱，不适合接收陌生人的任务或不可信项目。** AI 工具以执行主机上的当前用户权限运行。目录校验、OpenCode 权限规则、Node 配对和用户确认都不能代替操作系统隔离。

## 项目、执行和 AI 审批

- 项目可以是当前 Windows 用户可访问的普通文件夹，**不要求 Git 仓库或干净工作区**。注册时校验实际目录并要求确认信任，不建立 Git 基线、文件快照或内容哈希；只有项目自身依赖 Git 时才需要 Git。
- 新会话的本机发送明确请求入队执行，按本机项目、模型和审批设置启动；升级前未申请执行的本机遗留 open/ready 任务不会因为新队列而自动开始。指定 `@Node` 后绑定稳定 Node ID，普通忙碌仍在该 Node 排队，离线或不可收件时明确提示，不静默转到本机或其他 Node。
- 设备信任、接收任务、本机执行能力和 AI 操作审批分别控制。受信任务默认自动收件；执行能力关闭或资源尚未配置时等待。接收 Node 自主管理项目、模型、凭据、审批模式和最终准入，信任设备不等于授权对方任意调用本机业务 API。旧版按 Node 允许列表和逐项接受邀请的执行策略已被当前配置替代。
- 每项任务锁定创建时的审批模式，使用官方 OpenCode 会话权限规则：

| 模式 | 当前规则 |
| --- | --- |
| 请求批准 | 项目内修改和命令请求审批；网络检索/请求也需审批；项目外目录访问拒绝 |
| 帮我批准 | 自动批准项目内修改和命令；网络检索/请求仍需审批；项目外目录访问拒绝 |
| 允许任何操作 | 自动批准修改、命令、网络检索/请求及项目外目录访问 |

三种模式都拒绝子代理、技能加载和匹配 `.env`、私钥、`auth.json`、credentials 等规则的直接文件读取。**这些是工具权限规则，不是对任意 shell 命令的完整隔离或秘密防泄漏保证。** 已允许的命令、项目配置、插件和依赖可能产生超出预期的读写、联网及其他副作用。自动审批会调用模型并可能修改文件，不是无风险通知。

## Node 信任、网络与远程控制

- React 业务服务和官方 OpenCode 只监听 IPv4 loopback；OpenCode 每次启动使用随机 Basic Auth 密码，不向 WebView 返回密码，也不提供通用引擎 API 透传。局域网只开放独立的最小节点端点及发现服务，不能将业务端点或 OpenCode 暴露到局域网或公网。
- 每个实例拥有稳定 Ed25519 节点身份。私钥用 Windows DPAPI CurrentUser 加密落盘，Node ID 与公钥指纹绑定；身份文件损坏或换用户无法解密时不会静默生成新身份。签名证明节点密钥身份，不能替代信任或人员授权。
- mDNS/DNS-SD 与 LAN UDP 查询用于发现候选。候选仍须经过来源地址、随机挑战、Node ID、公钥和签名核验；广播本身不能授予信任。节点公开接口仅处理受限的发现、身份、配对和撤销，不提供项目、模型、凭据或业务任务查询。
- 配对绑定双方身份与随机会话，并有时限和重放检查；六位短码仅供人工核对，不作为密码。双方分别确认后才保存信任。信任冲突或损坏时拒绝静默重置。
- 受信节点以 Ed25519 签署临时 X25519 握手，经 HKDF-SHA-256 派生方向密钥，使用 AES-256-GCM 保护消息。握手、消息序号、时间有效性、来源地址与当前信任共同校验；会话密钥不持久化。普通节点请求、加密信封和消息正文有各自的大小限制，并非所有消息均为 16 KiB。邀请期限也不代表已接受的任务到期后必然停止。
- 认证加密通道已支持 Brain/Worker 目录、任务投递、执行状态、远程一次性批准/拒绝、回答问题、停止、补充要求并继续、官方会话差异以及验收。远程入口要求本机所有者与确认；操作校验原任务路由、控制 ID 和预期执行序号，同一任务串行处理。重启后已开始但结果未知的控制不会自动重放。接收端通过执行节点的本机操作者处理控制，当前未建立独立的跨设备人员身份/企业角色体系。
- 指定 Node 复用直接任务路由；自动分配沿 BrainTask/Execution 路由，Task 的 Brain 归属保持。当前 Master Host 固定，没有选主、HA 或跨 Brain 迁移；多个 Brain 通过 Worker 共享资源。
- 受信节点目录包含 Node 显示资料、配置项目的 ID/名称及硬件/负载能力，本地备注不出站。新增队列公共统计只含等待数、暂停、健康及时间信息；单任务排位/原因按原路由返回对应来源，不广播其他来源的完整队列或任务正文。资源报告和排序不提供操作系统配额或资源预留保证。
- 执行状态中的摘要、审批、问题和差异按字段限制并尽力脱敏，移除审批 metadata，已知本机项目根目录替换为 `<project>`；执行状态不专门传送本机业务 Task ID、项目绝对路径、模型标识或凭据。控制正文有长度和形状校验，不能视为已自动去除秘密或项目路径。任务正文、Node 名称及工具输出本身仍可能含敏感信息，不能保证识别所有其他绝对路径或秘密。不要把执行状态的字段限制扩大理解为目录不共享项目 ID/名称，或整个通道具备完整 DLP。
- 新队列能力节点的送达确认绑定原请求并经认证，裸 HTTP 204 不能证明送达。旧能力节点保留基本任务兼容，但不提供新队列字段；目录或回执缺失、过期、断线时不能推断实时排位或送达成功。队列回执使用独立序号，不以两台机器的时间戳直接判定先后。
- 撤销先在本机持久生效并清除通道，再通知对方；离线时重试通知。撤销阻止尚未开始的调用，并请求停止该来源在本机仍运行的任务；旧撤销不能覆盖更新的重新配对关系。已经发生的文件、网络和系统副作用不会回滚。

## 本机身份、权限与凭据

- 桌面把当前 Windows 用户视为本机操作者，使用每次启动的临时本机认证令牌和精确窗口/origin 校验建立会话。令牌不写入业务 SQLite、日志或前端持久化，退出时清理，重启失效；普通浏览器不能通过桌面命令取得它。同一 Windows 账号下的进程属于同一信任边界，桌面没有应用内切换人员身份的入口。
- 内部 Web 调试保留初始化码、账号和服务端角色验证；密码使用 scrypt 加盐，会话令牌仅保存 SHA-256 摘要，HttpOnly、SameSite=Strict cookie 有时限。Cookie 不带 Secure，因为业务只在本机 HTTP 使用。浏览器变更接口检查 Origin、自定义请求头与 Host，并有限速、CSP、禁止 iframe 和 nosniff；没有宽松 CORS。
- 本机任务与 SSE 按参与者过滤；普通成员的网络任务/回执视图也按实际参与的本机任务绑定过滤。全 Node 队列查询、排序、暂缓/恢复、拒绝和暂停由本机所有者控制。项目列表、目录、成员名称和执行策略仍可供本机普通登录成员读取；早期账号协议不是面向陌生人的多租户系统，没有组织级项目 ACL、MFA 或完整成员管理。
- 项目、模型凭据、默认模型和连接测试由本机所有者管理。提供方凭据属于执行 Node；受信任务可能消耗其账号额度，没有逐成员凭据隔离、消费硬上限或账单保证。连接测试限制次数、禁止工具且不自动重试，仍可能产生费用。
- 模型 Key 通过官方 OpenCode 认证接口在独立引擎目录管理。业务数据库和模型操作接口不保存或返回完整 Key；输入提交后清空。程序不自动导入个人 OpenCode 凭据，只在显式导入时读取指定来源。
- 数据目录包含业务 SQLite、任务/信任/队列控制等本机文件以及独立引擎数据。除 DPAPI 节点私钥外，没有完整的静态数据加密；任务正文可在本机 JSON/SQLite 中以明文保存。目录主要依靠 Windows 权限保护，不应提交或同步到公开位置。开发默认 `.data`，桌面使用自身应用数据目录，不能把源码目录当作所有安装实例的实际数据位置。
- 引擎使用独立 XDG 数据、配置与缓存目录；子进程仅继承必要系统环境和代理设置，不继承整个应用环境。引擎原始数据及日志仍可能含敏感内容；不要在任务、项目或报告中填写生产秘密。

## 队列、结果与恢复限制

- 队列顺序与操作持久保存，操作有版本和唯一 ID 校验；创建请求按发起人及 requestID 幂等。Brain Task、远程 Execution、本机 Task 和 OpenCode session 是不同身份，不能理解成跨数据库/引擎的单一事务或严格一次执行承诺。
- 最终准入重验信任、执行策略、模型、槽位和任务状态。运行、待审批/输入、review、interrupted 或准入预留会占用相应槽位，不能通过调序或重启假装释放。普通忙碌和短时高负载仍允许向固定目标排队，资源健康提示不等于所有高负载都拒绝接收。
- 暂停队列阻止未准入项继续准入；已获得执行槽位或正在执行的任务不因此停止。暂缓/拒绝只作用于尚未准入的等待项；运行中使用停止操作。停止无法确认时保持中断/未知状态，不把通信失败当作已停止。停止不能撤销已发生的文件、网络、费用或系统操作，对脱离进程树的后台任务也没有可靠终止保证。
- 同项目任务串行，但不会阻止其他编辑器、终端或进程同时修改文件；没有首轮干净工作区或最终文件哈希一致性保证。人工验收校验角色、任务状态和版本，**不校验磁盘文件内容哈希**，验收人应直接检查真实文件和测试结果。
- 产物与差异仅展示官方 OpenCode 会话差异，敏感文件/二进制内容不展示，远程结果另有限长。官方未返回差异时明确提示本地检查；不以 Git diff、文件扫描、快照或内容哈希兜底。差异页不是完整副作用清单，也不是所有修改都已安全验证的证明。
- 应用使用自有进程监护与受控退出清理 OpenCode 子进程。重启后已开始但未确认的执行转为中断，不自动重试或验收；已授权且尚未开始的等待项可按持久队列继续。引擎 session 创建意图已持久保存但绑定结果未知时保守阻止重复创建，仍需人工检查现场。
- SSE 可重连，任务状态由刷新补齐，没有无限事件回放保证。发现在线状态只是提示，不能当作执行成功、故障转移或安全授权依据。纯 mDNS 专项仍有已记录失败；不能据正常 mDNS+UDP 路径通过推导出所有网络环境可用。

## 试用与发行边界

**不要把生产凭据、个人敏感文件或有价值的运行环境放在执行主机上。** 注册前审核项目及其配置，仅在可信专用网络配对。局域网发现会暴露节点存在和部分标识；防火墙放行、私有地址过滤和限频均不是隔离。AI 请求会将提示、代码及工具结果发送给所选模型提供方。

既有普通文件夹真实模型执行和 M3.3/M3.4 的记录范围已通过验收；M3.5 已完成同机隔离服务、session 创建崩溃窗口及原生界面检查，用户验收与本轮双物理机回归待进行。源码仓库 `docs/VERIFICATION.md` 保留逐轮通过、失败和未覆盖边界；确定性测试模型不算新增真实模型编程或两真人验收。

当前独立预览未签名，没有接入应用内自动更新；构建成功不代表安装、干净机/升级或商业发行验收完成。签名与安装升级矩阵、两真人权限回归、停止/撤信的其他故障边界、独立低权限执行账号、成员治理、日志保留/清理和备份恢复演练仍需按目标发行范围推进。已有两处 session 创建故障注入不代表磁盘满、断电、数据库损坏或长时间压力场景均已覆盖。备份需先停止应用，再保存完整实际数据目录；项目文件需另行管理。
