# 内测安全边界

此 MVP 只适合可信参与者和专用 Windows 测试环境。**不提供安全沙箱，不适合接收陌生人的任务或不可信仓库。** AI 工具以执行主机上的用户权限运行。目录校验、Git 根目录要求、OpenCode permission 规则和用户确认均不是操作系统隔离。

## 已实现

- 业务服务与 OpenCode 只监听 IPv4 loopback。引擎每次启动生成随机 Basic Auth 密码，只存在进程内存/环境中，不传给浏览器，不提供引擎 API 透传。
- 每个安装实例生成稳定 Ed25519 节点身份；私钥 PKCS#8 使用 Windows DPAPI CurrentUser 加密后落盘，公钥哈希同时作为 Node ID 和人工核对指纹。身份文件损坏或换用户无法解密时拒绝静默生成新身份。
- M3.1 仅在局域网发布 `_rivloom._tcp.local` DNS-SD 服务，并开放独立的最小节点端点 `GET /v1/hello`。端点只接受私有/本机来源，带限速、请求超时和大小上限，只返回公开节点/Brain 标识、能力、时间和对随机 nonce 的签名；不返回账号、任务、项目路径、模型、凭据或业务 API。
- 发现探测只连接实际发送 mDNS 响应的私有 IPv4 源地址，不跟随广播中指向其他内网主机的地址记录。结果必须通过 Node ID、公钥指纹和 Ed25519 签名校验。签名身份只证明“这是同一个节点密钥”，**不表示可信或授权**；当前附近节点始终保持未配对，不能读取或委派任务。
- 一次性本机初始化码；一次性、24 小时邀请码；scrypt 加盐密码；随机会话令牌仅保存 SHA-256 摘要。HttpOnly + SameSite=Strict cookie，12 小时有效；退出删除会话。
- 变更接口要求同源 Origin（浏览器请求）和自定义请求头；严格 Host 白名单；无宽松 CORS；认证接口限速；生产界面 CSP、禁止 iframe、nosniff。
- 任务成员可见性和四类角色在服务端验证；SSE 事件按任务参与者过滤。项目只能由工作区创建者注册。同项目执行串行，人工操作排队，后台同步避免占用操作锁。
- 修改和 shell 命令每次审批。禁用子代理、外部目录、网络检索等；拒绝常见 `.env`/密钥文件读取。只保留官方默认构建 Agent，不设计自研 Agent。
- 凭据与业务状态在 `.data`；引擎使用独立 XDG 数据/配置/缓存目录，子进程只继承必要系统环境与代理设置，不继承整个应用环境。
- DeepSeek Key 通过官方 OpenCode `auth.set` / `auth.remove` 管理。业务数据库和界面只保存配置时间、验证状态及无密钥操作记录；不返回完整 Key、提供方错误体或测试回复。保存/替换后输入框立即清空。
- 只有工作区创建者能修改模型凭据、默认模型或发起连接测试。凭据变化和连接测试会避让执行中/中断未处理的任务；真实测试禁止全部工具、60 秒超时、不自动重试，并限制为 10 分钟 3 次。
- 常见令牌格式和键值进行尽力脱敏；敏感文件名/二进制 diff 不展示；前端文本使用 React 默认转义，不渲染 AI HTML。
- Git 产物包含 tracked 修改和非忽略的新文件，验收时核对内容 SHA-256。文件特殊类型/超大差异拒绝自动验收，不静默截断。
- 应用异常退出时，独立进程监护器检测 IPC 断开，结束自己拥有的 OpenCode 进程树。重启将未完成任务转为中断，不自动重试或自动验收。

## 必须理解的限制

- **不要放生产凭据、个人敏感文件或有价值的运行环境在执行主机上。** OpenCode 配置、仓库插件、工具指令可能影响执行；注册前由创建者审核。恶意任务、命令、依赖安装仍可能危害本机。
- 全部可信内测成员可以看见项目路径、项目列表和成员名称，可以对已授权项目创建任务。不是面向陌生人的多租户系统；没有组织层级、项目 ACL、MFA、禁用账号、密码重置和邀请撤销界面。
- `.data` 主要依靠 Windows 目录权限保护；节点私钥额外使用 Windows DPAPI CurrentUser 加密，引擎凭据仍采用官方 OpenCode 文件格式，业务数据库和其他数据没有完整本地加密。其他同机用户/进程的访问权限必须由 OS 控制。不要同步或提交该目录。
- 当前提供方凭据属于整台执行主机/工作区。可信成员提交的任务会消耗创建者配置的额度；不是每成员独立凭据、额度隔离、消费上限或账单系统。连接测试即使停止也可能已产生费用。
- Cookie 没有 Secure 标志，因为业务应用只接受本机 HTTP，且不会发往节点发现端点。M3.1 的局域网 HTTP 只传公开身份和随机挑战，不承载 Cookie 或敏感业务数据。配对后的任务通信必须在后续阶段另行设计加密、认证、重放保护和撤销；当前不得开放业务端点到局域网或公网。
- mDNS 广播和节点端点会让同网段设备看到 Rivloom 节点及其 Brain 标识；Windows 防火墙也可能提示允许随包 Node.js 访问专用网络。内测只允许专用可信网络，不应在公共 Wi-Fi 放行。来源地址过滤不是隔离，也不能替代配对和加密。
- AI 请求会将提示、代码和工具结果发给选定模型提供方。脱敏不是完整 DLP：未知格式、分段流式输出、输出文件与引擎原始日志仍可能包含秘密。不要把脱敏当作上传秘密的许可。
- 停止不回滚已经发生的文件/网络/系统操作。实际验证了等待审批期间停止并阻止写入；长期 shell 命令、已脱离进程树的后台任务和外部副作用没有可靠终止保证。
- 产物只涵盖 Git 可见文件。忽略文件、仓库之外的副作用需要人工检查；二进制/敏感文件只显示提示，不能仅凭在线 diff 验收。
- 不检测其他编辑器或终端实时写入：只要求首轮工作区干净、同项目串行及最终验收指纹一致。验收检查与状态落盘不是 OS 文件事务，请在验收时暂停其他写入者。
- OpenCode 的会话 diff 在本机 1.18.25 实测为空，使用只读 Git diff 作为明确降级。不是修改引擎后的“修复”。
- SSE 断线自动重连，任务状态靠轮询补齐；不是无限事件回放系统。长任务输出、大量任务、磁盘满、断电、数据库损坏尚未做压力与灾难恢复测试。备份需先停止应用再备份完整 `.data`。

## 发布前仍需做

有效 DeepSeek Key 的真实调用、两台物理设备的发现验证、明确的设备配对/撤销与加密节点通信、两真人跨设备任务内测、干净 Windows 安装/升级与签名、独立低权限 OS 执行账号、可撤销的成员管理、日志保留/清理、备份恢复演练，以及按预期部署方式做专项安全评审。不要把同机双实例发现写成物理设备已经验证，不要把签名发现写成可信授权，不要把测试字符串凭据生命周期检查写成 DeepSeek 已连接，也不要把本 MVP 当作生产安全边界。
