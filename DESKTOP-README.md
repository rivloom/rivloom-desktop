# Rivloom 使用说明

Rivloom 是 Windows x64 上的人与 AI 任务工作区。本包版本为 0.1.18，已携带 Node.js 和由 Rivloom 从固定源码编译的 OpenCode 引擎，无需另外安装这两项；项目自身需要的工具链和模型提供方网络仍需准备。应用内签名更新频道已上线。Windows Authenticode 代码签名尚未配置。

从 0.1.4 起，对外安装包和 Release 统一使用 Rivloom 名称，应用身份为 `com.rivloom.desktop`。此前 Rivloom UI Preview 使用独立身份，安装新版不会自动导入它的会话或设置；旧 Preview 安装和数据继续保留。请先在新版确认所需配置，勿把改名视为旧数据迁移或升级兼容性已经验收。

## 软件更新

应用启动后和每 6 小时自动检查官网版本；也可点击左下角“关于 Rivloom → 检查更新”。选择“跳过此版本”后不会再次自动提示同一版本，手动检查仍可安装。选择“下载并安装”后显示进度，可后台下载或取消；安装前会等工作结束、验证签名并备份应用数据，完成后重新打开。

有运行、等待处理的任务、队列或未完成协作时，会说明原因并提供“重试安装”，不会为了更新直接停止工作。旧 0.1.4 需先手动安装首个支持更新的版本。详见 [应用内更新说明](docs/DESKTOP-UPDATES.md)。

## 关闭窗口与退出

点击窗口右上角的关闭按钮后，Rivloom 留在任务栏右下角的系统托盘，任务继续在后台运行。首次关闭会显示说明，点击「知道了」后不再提示；选择「留在工作区」则取消本次关闭。

点击托盘中的 Rivloom 图标，或右键选择「打开 Rivloom」，即可回到工作区；再次启动 Rivloom 也会打开已有窗口。如图标被 Windows 收起，可在托盘的隐藏图标区域找到。要完全退出，请右键点击托盘图标，选择「退出 Rivloom」，应用会停止执行并直接退出，不再弹出确认框。窗口的最小化按钮仍按 Windows 默认方式工作。

## 开始一个会话

1. 打开「设备与模型 → 模型与执行」，配置可用模型及本机执行目录。规划和执行都会使用实际执行设备上的账号额度，模型调用可能计费。
2. 点击「新会话」，选择或添加本机可信的普通工作文件夹，再选择模型。文件夹不要求 Git；Rivloom 不建立 Git 基线或文件快照。
3. 在「会话设置」选择本机 AI 审批模式，按需填写完成要求，输入任务并发送。系统先分析与规划，再自动安排步骤和执行 Node。本机发起的任务不设固定并发上限；其他机器发来的任务按本机配置的并发名额执行，满额后等待。
4. 在原会话处理 AI 的工具审批、提问和队列确认。成功执行自动完成，无需额外人工验收；仍应核对实际文件和验证结果。多步骤任务展示可折叠任务图，可以选择步骤查看当前或历史执行及文件，也可修改尚未开始步骤的要求、首选 Node 和依赖。

修改会直接留在所选文件夹。「产物与差异」只展示 OpenCode 实际返回的会话差异，可能没有可用差异；请直接检查执行设备的文件和测试结果。Rivloom 不自动提交、推送、回滚或部署。

## 指定另一台 Node

在「设备与模型 → 节点与 Brain」完成配对，两端核对短码或指纹并分别确认信任。发现设备不等于信任设备；设备信任也不等于人员授权或 AI 操作批准。

新会话不指定 Node 时，系统按资源、工具和依赖自动安排。输入 `@` 并选择 Node 表示首选，条件不适合时可自动选择其他合适设备；输入 `@@` 表示所有执行锁定在该 Node，仍可查询并取回其他节点允许访问的材料。选择器和目标标签均可切换模式；用标签的清除按钮恢复自动选择。无论哪种模式，都会先形成实际计划。

Windows 桌面端的「设备与模型」和「连接诊断」会检查本机的网络类型、通信监听及有效防火墙规则。应用实际通信程序为安装目录中的 `runtime/node.exe`。缺少适用规则时，可点击「允许局域网连接并复测」，通过 Windows 管理员授权仅为当前安装配置局域网入站规则；默认覆盖专用/域网络，公用网络须明确选择。该操作不关闭防火墙、不更改网络类型或组织策略，也不会绕过设备身份验证。规则已匹配不代表对端可达，两台电脑都应分别检查。

修复后会重新发现设备并读取连接状态，不会自动确认配对或派发任务。查询失败保留的结果明确标记为上次记录。规则按安装路径标识，自动更新保留已有授权；卸载只清理当前安装创建的规则，用户手动创建的规则不会被删除。取消清理授权时卸载停止，便于稍后重试；静默卸载不会弹出授权窗口。

受信设备发来的任务按已有准入规则自动接收，使用接收端预设的目录、模型和审批设置。实际目标已有 10 项排队或执行中的工作时，原会话会要求确认是否继续排队。锁定的 Node 离线或条件不足时等待，不绕过锁定；状态不确定的已接收执行不会自动重派。新流程要求参与节点都支持协作功能，旧节点仍可使用原有任务协议。

## 资源目录与转交

当前协作任务的「步骤状态」会分别显示执行、排队、准备材料、等待前置步骤和待确认的执行。尚未派发的步骤可展开查看各设备的条件：连接、执行配置、工具报告或硬件要求；报告缺失或过期时明确显示待确认。报告时间与本次检查时间分开显示，设备总负载不会被当成本任务排位或预计开始时间。

执行设备的排队回执会区分等待、暂缓、已获执行槽和拒绝；已暂缓的工作需到执行设备的队列恢复。尚未确认执行结束时，查看回执不会触发另一份执行。面板底部可展开「诊断摘要」并复制状态、报告时间和匿名设备编号，步骤编号按计划顺序排列。摘要省略任务正文、标题、设备名称、路径和回执自由文本；刷新失败时标记为旧记录。复制不可用时可选中摘要文字手动复制。

可从步骤旁打开对应设备的连接诊断、进入本机模型与执行设置，或修改允许编辑的未开始步骤。诊断页可返回原会话。「刷新状态」只重新读取事实，不重新提交任务。条件恢复后，未派发步骤继续检查；已入队或状态不明的执行仍留在原设备核对。失败步骤沿用原重试条件，保留已完成成果；用户暂停或停止的任务不会因设备恢复而自行继续。旧设备提供的信息不足时显示待确认，不猜测其具体配置。

配对后，需先在各 Node 选择本机执行目录，才能登记其中可发现的文件和工具信息。未选择目录时不会自动扫描其他位置。「模型与执行」中的资源目录可查看扫描/离线/部分结果状态，并按文件名、路径、类型或工具名查询；普通目录查询不创建执行任务或模型会话，正文和素材由任务按需获取。

流程中的实线表示步骤依赖，转交虚线表示同一步骤换到另一台 Node。系统只交接已保存并验证的业务文件和上下文，不搬运运行中的进程或 GPU 内存；无法独立确认源工作停止时会阻止自动转交。原执行和结果保留在原会话。

「暂停派发」阻止新步骤发出，已进入某 Node 队列的执行仍会继续；「停止整个任务」向已有执行请求停止，无法确认时明确保留未知状态。未开始步骤的编辑以打开表单时的版本为准，期间流程变化需重新打开核对。已完成步骤不会因编辑自动重跑。

## AI 审批模式

每个任务会锁定创建时选定的模式。设备配对确认与下列 AI 工具审批互相独立。

| 模式 | 当前行为 |
| --- | --- |
| 请求批准 | 允许读取和搜索；修改、命令及联网操作需要批准。 |
| 帮我批准 | 自动批准项目内修改和命令；联网仍需批准。 |
| 允许任何操作 | 在支持的工具范围内，还自动批准联网和项目外目录操作。 |

三种模式仍保留敏感凭据文件、子代理和上游原生 Skill 等工具限制。Rivloom 管理的共享 Skills/Wiki 属于尚未发布的开发功能，使用独立的任务权限与手动分享机制。模式名称不代表操作系统级隔离，详细范围见同目录的 [SECURITY.md](SECURITY.md)。

## 队列与停止

在「本机队列」可调整等待项顺序、暂缓、恢复或拒绝尚未获得执行槽位的会话。「暂停队列」阻止这些等待项取得槽位，「暂缓」只让该等待项暂不参与执行；已经获得槽位或正在运行的任务不因此停止。

本机任务与其他机器任务分别排队。本机任务不设固定并发上限；其他机器任务默认最多同时执行 3 项，可在队列面板的「修改」、模型与执行页或 Brain 页修改为 1–10 项。界面直接显示当前已保存的值，修改后点击「保存并发设置」生效。降低上限不会中断或撤销已有执行，新增外来任务等待名额空出。旧版本固定 1 的配置升级后改为默认 3，已有执行权限、目录、模型与审批选择保持。

要终止正在执行的工作，请在会话中点击「停止」，并等待停止结果。停止不会撤销已经发生的文件修改或命令效果；未确认停止时，请继续查看会话状态。

## 使用边界

Rivloom 不是安全沙箱。AI 工具和命令使用当前 Windows 用户权限，命令仍可能访问项目外文件或网络；应在可信环境和无敏感信息的测试文件夹中使用。提示词、项目内容和工具结果可能发送给所选模型提供方，请勿把密钥或其他敏感信息放入任务。

完整安全说明见 [SECURITY.md](SECURITY.md)，第三方开源声明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。同目录的 `runtime-manifest.json` 记录了本说明与安全说明的 SHA256，可用于核对随包文档。
