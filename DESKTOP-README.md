# Rivloom UI Preview 使用说明

这是 M3.5 会话式 Node 协作的 Windows x64 预览版。安装包已携带 Node.js 和官方 OpenCode，无需另外安装这两项；项目自身需要的工具链和模型提供方网络仍需准备。当前预览未签名，未接入自动更新，也没有下载或更新服务；工程验证已完成，用户验收及本轮双物理机回归仍待进行。

## 开始一个会话

1. 打开「模型与额度」，配置本机可用的模型及凭据。任务会使用执行设备上的账号额度，模型调用可能计费。
2. 点击「新会话」，选择或添加本机可信的普通工作文件夹，再选择模型。文件夹不要求 Git；Rivloom 不建立 Git 基线或文件快照。
3. 在「会话设置」选择 AI 审批模式，按需填写完成要求，输入任务并发送。会话会保存到执行队列，条件就绪后开始；设备忙碌时等待槽位。
4. 在会话内处理 AI 的审批或提问。AI 执行结束后进入「待验收」；核对实际文件和测试结果后，再点击「确认完成」并记录核对结果。需要修改时可在原会话补充要求。

修改会直接留在所选文件夹。「产物与差异」只展示 OpenCode 实际返回的会话差异，可能没有可用差异；请直接检查执行设备的文件和测试结果。Rivloom 不自动提交、推送、回滚或部署。

## 指定另一台 Node

在「节点与 Brain」完成配对，两端核对短码或指纹并分别确认信任。发现设备不等于信任设备；设备信任也不等于人员授权或 AI 操作批准。

新会话中输入 `@` 并选择目标 Node 后再发送。受信设备发来的任务会自动接收，使用接收端预设的项目、模型和审批设置；接收端关闭执行能力时，任务会等待。信任另一台设备前，应确认愿意让它使用本机项目和模型账号额度。

发送后可在原会话查看送达回执、排位和等待原因。普通忙碌会继续排在所选 Node；目标离线会明确报错，不会自动改派到其他设备。

## AI 审批模式

每个任务会锁定创建时选定的模式。设备配对确认与下列 AI 工具审批互相独立。

| 模式 | 当前行为 |
| --- | --- |
| 请求批准 | 允许读取和搜索；修改、命令及联网操作需要批准。 |
| 帮我批准 | 自动批准项目内修改和命令；联网仍需批准。 |
| 允许任何操作 | 在支持的工具范围内，还自动批准联网和项目外目录操作。 |

三种模式仍保留敏感凭据文件、子代理和技能等工具限制。模式名称不代表操作系统级隔离，详细范围见同目录的 [SECURITY.md](SECURITY.md)。

## 队列与停止

在「本机队列」可调整等待项顺序、暂缓、恢复或拒绝尚未获得执行槽位的会话。「暂停队列」阻止这些等待项取得槽位，「暂缓」只让该等待项暂不参与执行；已经获得槽位或正在运行的任务不因此停止。

要终止正在执行的工作，请在会话中点击「停止」，并等待停止结果。停止不会撤销已经发生的文件修改或命令效果；未确认停止时，请继续查看会话状态。

## 使用边界

Rivloom 不是安全沙箱。AI 工具和命令使用当前 Windows 用户权限，命令仍可能访问项目外文件或网络；应在可信环境和无敏感信息的测试文件夹中使用。提示词、项目内容和工具结果可能发送给所选模型提供方，请勿把密钥或其他敏感信息放入任务。

完整安全说明见 [SECURITY.md](SECURITY.md)，第三方开源声明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。同目录的 `runtime-manifest.json` 记录了本说明与安全说明的 SHA256，可用于核对随包文档。
