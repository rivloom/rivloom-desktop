# Rivloom 桌面仓库工作约定

- 开始前核对 Git 状态并保留现有改动。若本机存在 `docs/NEXT-SESSION.md` 和 `docs/HANDOFF.md`，先读取；历史记录中的版本和待办不能覆盖文档顶部的当前状态。它们是仅本地保留的交接文件，全新克隆没有这些文件时，从 `README.md`、`docs/README.md` 和当前源码接手，不要求恢复或上传内部记录。
- `docs/` 默认由 `.gitignore` 忽略，只有明确列出的公开文件及 `adr/`、`releases/`、`licenses/` 放行；交接、进度、验证与性能记录、官网维护资料、`docs/plans/` 仅保留本地，禁止使用 `git add -f` 上传。公开文档不得链接这些本地文件；公开说明、架构决策、发布流程、版本说明、依赖清单和许可原文继续受 Git 跟踪。内部记录由维护者另行备份，不能只依赖 GitHub 恢复。
- 涉及发布、官网分发或 R2 维护时，必须读取 `docs/CI.md`、`docs/RELEASING.md` 和 `docs/R2-RETENTION.md`，沿用既有安装验收、公开下载与签名核验流程。
- 每次正式发布都必须执行并记录 R2 保留策略检查：保留最近 3 个正式版本、近 30 天发布的包，以及当前下载页和更新清单引用的文件，取这些集合的并集；信息不完整的对象保留。禁止对整个桶设置不区分当前版本的到期删除规则。
- R2 清理按 `docs/R2-RETENTION.md` 的顺序执行。现有 CI 尚无自动清理功能，自动化接入前由发布执行者完成检查与收尾；不得把“文档已规定”写成“自动清理已上线”，也不得把未执行或失败的清理记为成功。
- 发布收尾必须记录保留、删除和暂缓的对象及理由；没有满足条件的旧包时记录“检查完成，删除 0 个”。R2 策略不授权删除 GitHub Release、用户安装版、真实项目或应用备份。
