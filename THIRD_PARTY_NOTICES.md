# Third-party notices

The current Windows and Linux x64 source builds use Rivloom's OpenCode fork based on upstream **1.18.31**, with the matching TypeScript SDK/plugin. OpenCode is Copyright (c) 2025 opencode and distributed under the MIT License. The full upstream license is preserved in [docs/licenses/OpenCode-MIT.txt](docs/licenses/OpenCode-MIT.txt) and alongside each engine artifact.

The application is an independent integration, not an official OpenCode or OpenAI product. Its engines are compiled from a fixed [Rivloom runtime](https://github.com/rivloom/rivloom-opencode-runtime) commit, recorded in [the Windows source lock](shared/engine-source.json) and [the Linux source lock](shared/engine-source-linux.json). Linux also records the separately pinned build recipe snapshot from the runtime repository. This fork adds Rivloom build provenance, a separate plugin package version and an inactive prompt scaffold; it is not an unmodified official binary. Codex is not integrated. Distribution must preserve upstream copyright and license notices. This source change is not a claim that existing public installers have been replaced.

Runtime components also include React / React DOM (MIT), i18next / react-i18next (MIT), Express (MIT), Zod (MIT), Lucide React (ISC), and bonjour-service (MIT) for mDNS/DNS-SD node discovery. Their installed license texts and available transitive dependency license texts are retained in [docs/licenses](docs/licenses). Exact installed versions, licenses, development-only flags and integrity records are in [docs/dependency-licenses.json](docs/dependency-licenses.json); dependency resolution is locked in `package-lock.json`.

Brand labels bundle the unmodified Manrope Latin variable font from `@fontsource-variable/manrope` 5.3.0 under the SIL Open Font License 1.1. Its full copyright and license text is included in [docs/licenses/fonts/Manrope-OFL.txt](docs/licenses/fonts/Manrope-OFL.txt). The font is served from the packaged application; it does not request an external font service. The main interface and code views reference installed system fonts through CSS, including Segoe UI, Microsoft YaHei UI, Microsoft YaHei, Bahnschrift, DengXian, Trebuchet MS, Cascadia Code and Consolas where available. These system font files are not bundled or redistributed. Windows font names may be specified in a CSS font stack; this does not grant permission to copy or distribute the font files. See Microsoft's [font redistribution FAQ](https://learn.microsoft.com/en-us/typography/fonts/font-faq).

The Windows desktop distribution also includes unmodified Node.js 24.19.0 (MIT and bundled third-party notices in `runtime/Node-LICENSE.txt`), Tauri 2.11.5 and tauri-plugin-updater 2.11.0 (MIT OR Apache-2.0). Update verification also uses minisign-verify 0.2.5 (MIT), SHA-2, base64, SemVer, Tokio and reqwest with their recorded upstream licenses. Rust dependency resolution is locked in `src-tauri/Cargo.lock`; collected Rust license texts are in `docs/licenses/rust`, with an inventory in `docs/desktop-dependency-licenses.json`. Run `node scripts/desktop-notices.ts` after changing native dependencies. Microsoft WebView2 Runtime is a separate Microsoft component; the Windows installer can request Microsoft's bootstrapper when WebView2 is absent. No Windows Runtime license or model-provider service entitlement is granted by OpenCode's MIT license.

Build tooling includes Vite, TypeScript, the official React Vite plugin and Prettier. The engine compiler is pinned Bun 1.3.14; its source dependency lock and model catalog are recorded with the artifact. The generated inventory also records desktop development dependencies. Regenerate notices after changing dependencies with `node scripts/notices.ts`, and review missing or changed license information before distributing binaries. Dependencies compiled inside OpenCode retain their original terms; the desktop npm inventory is not a complete engine binary SBOM.

Rivloom's original application code is Copyright 2026 Rivloom contributors and licensed under Apache-2.0. See [LICENSE](LICENSE) and [NOTICE](NOTICE), both included in the packaged runtime. Third-party code retains its original terms. The package's `private: true` flag prevents npm publication; it does not select a license or control GitHub visibility.
