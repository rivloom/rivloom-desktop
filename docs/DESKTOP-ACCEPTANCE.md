# 桌面端实机验收记录

更新于 2026-09-08。用户最新要求暂缓剩余测试，之后再说；不继续安装验收、物理断网、性能或硬件边界测试，也不催促安装和配合操作。已完成的补测、历史栏改动和安装包保留。安装仍由用户操作，Git 等整体完成后统一整理。

本机已安装 EXE 的 SHA-256 为 `bf42c01d6bc69d585ab3f2c0dac71e50c71d37df5769de52ada662c32bb2c0e9`，与紧凑侧栏候选 `49b12819` 一致。5.29 已显示该轮侧栏和筛选界面，但未读取其 EXE 哈希。以下是已安装版的实际结果；随后新增的单行配色使用独立合成界面验证，详见[历史栏记录](plans/2026-09-08-history-density.md)。

## 本轮通过

| 项目 | 实际结果与边界 | 证据 |
| --- | --- | --- |
| 紧凑侧栏与中英文 | 实际窗口从 1614×1110 缩至 971×1110、971×710；筛选可打开，Escape 恢复可见焦点，底部栏目和输入区可用。中英文及原生标题切换正确；未测量拖动帧率 | `checks.json` |
| 原生目录选择 | 英文目录选择器确认后返回专用目录；再次打开后 Escape 取消，原值保留；最终取消网页表单，没有新增项目 | `checks.json` |
| 选择器打开时后台响应 | `/network` 请求 6.45ms，双方资源采样时间推进；不等同于其他 Tauri 原生命令并发测试 | `native-folder-open-network.json` |
| 强制崩溃恢复 | 专用任务 running 且 promptAsync 已确认时，结束已核实的客户端进程树；重启显示 interrupted，原 session 与保留槽位不变，无自动重试。显式继续返回恢复标记，随后验收 | `crash-before-kill.json`、`crash-no-auto-retry.json`、`crash-recovery-accepted.json` |
| 接收端半传文件恢复 | 双机传送 20MiB 合成文件。观察到已收 32KiB 后结束接收端，退出后的磁盘残片为 256KiB。重启后同一 Task/File 自动完成 20MiB，逐字节和 SHA-256 一致，仅一份附件；未抓取线上续传偏移 | `transfer-kill-result.json`、`transfer-stopped-blob.json`、`transfer-verified.json` |
| 原生另存确认 | 5.29 将合成附件保存到专用验收目录，实际界面显示保存路径；再次通过原生打开选择该文件并发回本机 | 本轮实际界面观察及 `transfer-verified.json` |
| 5.29 最小化通知定位 | 从连接诊断最小化，点击 Windows 通知中心中的定向任务待验收通知，恢复到原返回任务，结果标记保留 | `checks.json`、`return-accepted.json` |
| 5.29 冷启动通知定位 | 正常退出后图标消失，配对端观察到离线；点击 Brain 待验收通知启动并进入原任务。Execution、attempt=1、sequence=3 保持；未读取远端 PID/session | `brain-cold-peer-offline.json`、`brain-after-cold-click.json` |
| 5.29 运行中通知定位 | 从连接诊断点击 Brain 完成通知，进入原已验收会话，正文保留，队列为空 | `checks.json`、`brain-accepted.json` |
| Windows 拼音组合输入 | 逐键输入 ni 显示候选，空格提交“你”；另一次组合期间 Enter 提交文字但不发送任务。任务数和队列不变，测试草稿清空；未测量输入延迟 | `checks.json` |

证据目录为 `.data/verification/remaining-acceptance-20260908/`。崩溃发生在模型请求确认后，不宣称已验证逐 token 输出中途被杀。合成附件 SHA-256 为 `a56d43d382907c23da8ec96e686613bd1db587ca84bba064d9ee64ba2cc92087`。

新增的崩溃、附件准备、附件返回和 Brain 通知四个测试任务均已验收或完成，双方队列为空。两台执行能力恢复关闭，本机恢复中文，草稿清空。`preserved-history-after.json` 确认原有 7 个本机、12 个远程、5 个 Brain 记录哈希一致，身份、原项目与配对 ID 保持。

## 保留待测（按用户要求暂缓）

- [ ] **真正物理断网**：专用任务运行时让 5.29 断网约 20 秒并恢复，核对离线诊断、原身份/Task/Execution、回执及无重复执行。用户已明确暂缓，之前的配合询问不再等待答复；退出造成的离线不代替本项。
- [ ] **完整性能实测**：连续拖动/缩放帧率、长会话滚动、输入延迟、执行中负载及显示缩放/刷新率等尚未量化。本轮仅证明窗口缩放和真实输入法功能可用。
- [ ] **完整性能交互回归**：滚动停留/追随、审批/回答及队列暂停/恢复需在性能候选上逐项复核。本轮实际覆盖会话切换、继续、验收、附件和另一台三种通知场景；隔离逻辑/服务通过不替代这部分实机检查。
- [ ] **原生选择器边界**：另存取消、选择器/所属窗口关闭后的回调及其他 Tauri 异步命令并发尚未逐项测完。本轮目录确认/取消、另存确认及选择器打开时后端响应已通过。
- [ ] **通知完整组合**：本轮 5.29 的三种场景已通过；本机在新性能安装版上完整重测三种场景、中文/英文与限流组合仍待完成。
- [ ] **真实资源与历史规模**：有/无 GPU、NVIDIA 工具不可用、慢或挂起的 WMI/磁盘、休眠恢复、采样进程重叠、合成大历史首次索引与长时间内存/句柄趋势仍待专门验证。
- [ ] **新侧栏安装后显示**：单行配色及新增左右栏拖动已通过中英文 8 组无界面布局、60 项交互、254 项逻辑、6 项隔离接口及类型/双语/构建；最新 `3bf23807` 包尚未由用户安装。实际 WebView2 显示按用户要求暂缓，详见[拖动记录](plans/2026-09-08-resizable-sidebars.md)。

一次原生系统菜单操作期间出现短暂“请求超时”及节点采样过期，直接后端请求正常；恢复后未复现，原因未确定，保留 `native-menu-timeout-observation.json`。不据此断言已修复或持续存在。

## 此前结果

本地执行、双向定向任务、Brain 调度、首次附件、成果逐字节回传、验收后正文保留、队列暂停/恢复及正常退出后原队列恢复已通过此前安装版实测。

`93b2431c` 通知候选此前在本机完成运行中、最小化和冷启动三种实际通知中心点击，原 session/正文保持且无重复执行，证据位于 `.data/verification/notification-physical-20260907/notification-passed-summary.json`。通知仅显示通用状态正文；早期只读注册探针与实际成功冷启动不一致，不能以该探针断言正式注册缺失。

两轮源码性能与隔离测试依据见[桌面性能记录](DESKTOP-PERFORMANCE.md)和[底层性能记录](DESKTOP-NATIVE-PERFORMANCE.md)。249 项逻辑、11 项协议、10 项 Rust、12 项完整服务检查及生产构建已通过；合成数据库、模拟资源探针和 headless 测量不等同于 Windows 帧率、真实硬件或长时间运行结果。
