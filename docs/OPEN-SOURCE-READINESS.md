# 开源准备记录

## 2026-09-15 · 开源文档与 runtime fork 保护收尾

用户授权同步并提交开源文档，以及开启两个 runtime fork 的密钥扫描与推送保护。README 移除官网私有仓库行，加入免费的 Star History 动态图；贡献指南与 ADR 0011 已同步桌面公开、0.1.15 和共享 Skills/Wiki 已发行状态，官网交接纳入 Pages 部署与重复 Actions 停用结果。

两个公开 fork `rivloom/rivloom-opencode-runtime`（dev）与 `rivloom/rivloom-codex-runtime`（main）的 `secret_scanning`、`secret_scanning_push_protection` 已通过仓库 API 开启，并在重新读取时均返回 enabled。默认分支源码、上游归属、其他返回的安全设置及 Actions token 权限与变更前一致。没有修改引擎源码、构建工作流、签名或发布凭据；这次配置核验不代表两个 fork 的完整历史扫描或真实敏感内容拦截演练已经完成。

本轮仅维护文档，沿用已有 `[skip ci]` 提交方式，避免 main push 的基础 CI 完成后启动自动发行；工作流定义和正式版 0.1.15 保持。设置前后回读证据保存在本机忽略目录 `.data/verification/open-source-followup-20260915/`。此前桌面公开与 0.1.15 发行的通过结果见下一节；历史过程不覆盖当前状态。

提交前版本一致性、覆盖映射、69 项 CI 自检、416 项逻辑测试及 TypeScript/Vite 构建通过，逻辑测试无跳过。构建保留已有的大 chunk 提示。README 两种主题的 Star 图链接已返回包含本仓库标签的 SVG；本次不新增依赖或 Actions，也不使用 GitHub Token 访问图表服务。

**当前仍待完成：** 真实外部 fork PR 的审批、只读测试和合并门禁验收。两个自有 runtime 发行物尚未接入桌面，属于后续开发；合作邮箱继续暂缓。

## 桌面公开与 0.1.15 发行结果

**2026-09-15 发布后状态：** rivloom-desktop 已按用户明确授权以 Apache-2.0 公开，匿名仓库、main 源码及 LICENSE 核对通过；私密漏洞报告、秘密扫描与推送保护启用，main 及 ci-v*/v* 发布标签保护已生效。开源后原计费阻塞的发布步骤重跑成功，0.1.15 发布收尾完成。官网继续私有。真实外部贡献 PR 尚未运行，外部工作流需批准、只读 token 和发布来源限制已核验。用户已确认撤销分享，四处当前引用已移除，保留原仓库和历史；不得按下方旧状态重复确认撤销或公开。

## 2026-09-15 · 桌面仓库已公开

用户明确授权公开 rivloom/rivloom-desktop；实际公开源码为 97ab7937b272278f876070de66ca3acc745dbe21，Apache-2.0。匿名仓库 API、GitHub 页面、main 源码引用和 LICENSE 均核验通过。官网仓库仍为私有，两个 runtime fork 未修改。私密漏洞报告、秘密扫描和推送保护已启用，当前扫描告警列表为空。

新增两个提交扫描无命中；当前 1475 个跟踪路径仍只有此前确认的公开测试验签公钥命中。上次审计后新增 8 次 CI、34 份 artifacts；8 份日志和 33 份小型 artifact 的共 41 个归档取回，artifact 大小/SHA-256 对齐元数据，内部文件 CRC 通过。加上本次候选的 7 份原始报告，共 112 个文件完成补扫，无新增真实凭据。新候选整份 ZIP 下载曾超时，本轮没有将它写成完整安装包解包审计；原始构建/锁文件/runtime/隔离安装证据已交叉核对，旧版完整安装包审计继续沿用。

远端规则已生效：main 禁止删除和强推；一般贡献要求 PR、1 位 CODEOWNER 审核及 16 项现有 CI，保留组织管理员直接提交以沿用用户要求的 main 自动发布；ci-v*/v* 标签禁止修改和删除。GitHub 不接受内置 Actions app 作为创建规则豁免对象，因此未安装额外标签创建限制，创建权限仍由仓库写权限与既有官方 main 发布门禁约束。外部贡献工作流全部须批准，token 保持只读且不能批准 PR。真实外部贡献 PR 尚未创建，不把配置核验记作真实外部 PR 运行成功。

公开后对运行 34946471004 重跑失败任务，Release 发布步骤已经成功；签名/R2/官网收尾状态以 VERIFICATION 顶部为准。证据在本机忽略目录 .data/open-source-20260915/，包括 anonymous-verification.json、open-source-result.json、三份规则和增量审计报告。下方“尚未公开/未提交”是历史阶段，不覆盖本段。

## 以下为公开前准备历史

以下“私有”“尚未启用”“剩余步骤”描述当时检查点；桌面公开、发行和后续 fork 保护的当前结果以上方为准。

**2026-09-15 更新：** 用户已授权将共享 Skills/Wiki 与上述准备修复一起提交并升级，版本定为 0.1.15，按既有 main 自动发布流程执行。下方“提交/发行待确认”属于此前阶段，已获本次授权；实际发行结果见 VERIFICATION 顶部。桌面仓库可见性和远端保护设置尚未变更。

日期：2026-09-14；2026-09-15 完成补充审计及分享引用清理。桌面仓库仍为私有，本轮不代表已经公开、提交、推送或发行。正式版本仍为 0.1.14；Skills/Wiki 保留为未发布的工作区功能。**用户已确认在账号内撤销分享，当前四处链接引用已移除；匿名访问未独立验证，公开和发行仍待确认。**

## 用户已确认

- 桌面自有代码沿用 Apache-2.0，Copyright 2026 Rivloom contributors；上游版权与许可保留。
- 桌面准备公开，官网继续私有。两个 runtime fork 只检查配置，不修改引擎源码。
- 桌面公开时启用 GitHub 私密漏洞报告；当前尚未启用。
- 保留 main CI 通过后的自动发布，不改为仅手动/标签发布。因此这轮不推送 main，以免把开源准备当作正式发行授权。
- 用户选择取消 ChatGPT 方案对话的分享，保留原仓库和既有 CI/CD，不为该链接迁移仓库或重写历史。2026-09-15 用户明确确认“我确定已经删了”，据此记录为账号持有人已确认撤销分享，并移除当前四处引用。此前匿名 HTTP 请求超时、网页读取及浏览器服务失败的记录保留，不把超时或用户确认写成独立匿名验证通过。

## 四个仓库的只读核对

| 仓库 | 可见性 / 默认分支 | 状态 |
| --- | --- | --- |
| rivloom/rivloom-desktop | 私有 / main | 5 个工作流，准备公开 |
| rivloom/rivloom-website | 私有 / main | 独立官网，继续私有 |
| rivloom/rivloom-opencode-runtime | 公开 / dev | anomalyco/opencode fork；API 返回 0 个工作流与 0 次运行 |
| rivloom/rivloom-codex-runtime | 公开 / main | openai/codex fork；27 个工作流、209 次历史运行，已有开发分支，不是空白仓库 |

四个仓库默认 GITHUB_TOKEN 均为 read，禁止 Actions 自行批准 PR。两个 fork 未配置 repository secrets；这不证明历史从未有过凭据。Fork 的 secret scanning / push protection 当前未启用，工作流包括继承的发布入口；本轮仅记录，没有启用、停用或删改它们。Codex 的历史运行审查不属于桌面公开审计范围。

桌面私有仓库的 main 保护与 rulesets API 返回当前套餐限制；私密漏洞报告 API 当前不可用。CODEOWNERS 本身不强制审批。公开时须实际配置并核验保护规则和报告入口，不能把本文件视为远端已经生效的证明。

## 本地修复

- 统一 LICENSE、NOTICE、npm/Cargo 元数据和第三方声明；保留 npm private 标志。
- runtime 随包复制自有 LICENSE/NOTICE，清单记录哈希，构建门禁逐项核对原文并拒绝缺失或篡改；包含重新生成清单后仍拒绝篡改的回归测试。
- 重写 README，补贡献指南、Bug/功能模板、PR 模板和 CODEOWNERS；更新随包说明与安全政策，区分稳定版、开发功能及未来 fork 接入。
- 正式候选限制为官方仓库的 main 手动触发、ci-v 版本标签或同仓库 main push 成功事件；脚本再次拒绝非 main 分支。保留同一源码的三组 CI、安装验收与签名/R2 分层发布门槛。
- R2 只读清单限制官方仓库 main。新增工作流策略自检，检查外部 PR 无部署凭据、只读权限、固定 Action SHA、checkout 不保留凭据，以及发布产物来自当前运行的确切 artifact ID。

上述约束保护现有流水线，不能代替远端分支/标签保护，也不能阻止有权限的人通过修改整个工作流绕开规则。首次公开后的真实外部 PR 云端验证仍待执行。

## 内容审计范围与限制

原始证据仅保存在本机被忽略的 `.data/open-source-20260914/`，不把下载日志和完整清单提交到公开仓库。使用官方 Gitleaks 8.30.1（下载按官方 SHA-256 校验）、补充凭据/分享链接规则和人工复核；这不是所有秘密类型或个人信息的识别保证。

- 当前准备提交的文件及全部本地可达历史：63 个提交，1774 个历史 blob；远端 main 与 15 个标签的目标提交均在本地历史中。
- GitHub 桌面：102 次 Actions 运行，399 个 artifacts，15 个 Release 与 15 个标签。全部 399 份 artifacts 的大小和 SHA-256 均与 GitHub 元数据吻合。102 份日志与 384 份小型 artifacts 的 ZIP CRC 核验通过；其内部 1223 个文件已检查。日志没有 GitHub 预期摘要，只记录本机哈希及 CRC，不伪称有远端摘要比对。
- Release 小型附件：15 份 SHA256SUMS 全部取得，包括之前缺失的 0.1.11；每份大小/哈希与 GitHub 附件摘要吻合，内部安装包摘要也与对应 Release 一致。全量重新扫描无 Gitleaks 命中。
- 15 个 Release 安装器均取得并核对大小/SHA-256：14 个复用已验证本机发行缓存，缺失的历史 0.1.4 从官网不可变地址匿名取得。15 份大型候选 ZIP 复用相同安装器字节、按原压缩参数生成 payload，并读取远端原始头部、报告和目录；重建后整份 ZIP 的 SHA-256 与 GitHub 原附件完全相同。这里记录为按字节重建并验证，不称作 15 次完整网络下载。
- 15 份候选 ZIP 共 120 个条目均通过 CRC 检查，其中安装器与对应 Release 再次逐字节摘要比对；其余 105 份报告进入扫描。使用校验过官方 SHA-256 的 7-Zip 26.03，仅解包、不执行安装器或内含程序。15 个安装器解出 121,073 个文件，约 5.52 GB；加入当前 60 个与历史 99 个二进制对象后，按内容去重共 9,730 个文件（其中 153 个二进制）。纯文本及二进制 ASCII/UTF-16 可读字符串共 211.10 MB，已完成 Gitleaks 和补充规则扫描。
- 已发现的凭据模式对应合成测试值、AWS 官方公开签名样例、测试 Hook、固定 UI 预览密码和 HTML autocomplete 属性；不据此宣称不存在其他泄露。
- Gitleaks 历史与源码各报告 1 项：tests/fixtures/updater/public.pub 中的公开测试验签公钥，人工确认不是私钥。云端日志/报告与全部 Release 校验文件无 Gitleaks 命中。补充的安装包/二进制扫描报告 16 项 Gitleaks、9 项补充规则命中，逐项复核为 Zod/Tauri 测试或说明样例、SDK 字段、占位符、代码误报及官方 OpenCode 的公开 OAuth 客户端 ID。PEM 命中是含 XXXX 的文档骨架，无私钥材料；OpenCode 二进制与锁定的官方哈希一致。没有发现新的真实凭据，也没有添加宽泛忽略规则掩盖结果。
- 同一个 ChatGPT 方案分享链接此前出现在 4 份当前文档和 37 个历史提交中，15 个发布标签均含相关历史。当前四处引用已移除；用户确认已撤销分享，原有 Git 历史、标签、兼容测试和发行签名溯源保持原样。历史中的 URL 字符串仍存在，本次没有宣称清除历史引用；沿用用户保留原仓库与 CI/CD 的决定。

本次静态扫描不等于识别所有个人信息、任意加密/压缩二进制区段或验证所有运行时行为。NSIS 的 StartMenu.dll 清单所列大小与实际解包大小不同，15 个包均已逐项记录；解包返回成功，首次完整 archive test 通过，实际输出仍受路径、文件数和总大小限制。未把列表大小差异隐藏成逐文件大小完全相同，也未借审查运行旧安装器。

新增证据：`.data/open-source-20260914/all-downloads-verified.json`、`installer-corpus-manifest.json`、`installer-triage.json`、`gitleaks-installers.json` 与 `share-verification.json`。旧的下载失败报告保留为过程记录，最终状态以上述完整核验记录为准。

## 验证记录

本地 414 项逻辑、68 项 CI 自检、版本/测试分类、双语/类型/生产构建通过。runtime 准备及实际校验通过：221 个 npm runtime 包、316 个 Rust 依赖，包含自有 LICENSE/NOTICE 的 945 份许可文件。新增门禁与 runtime 针对性测试 23 项通过。

独立本地 Git 克隆覆盖本轮源码后，以全新 node_modules 完成 `npm ci --offline`、生产构建和 68 项 CI 自检；没有复制应用数据或已有 runtime。npm 对 msgpackr-extract 的安装脚本给出未批准提示，仍完成安装和构建；这里不把它等同所有原生扩展或云端安装器验收。保留已有前端大 chunk 提示。未调用真实模型、登录真实账号、访问用户安装版数据或执行发布。

## 公开前剩余步骤（历史清单）

1. 用户审查本轮改动，确认公开源码中是否包含尚未发布的 Skills/Wiki。保留原有开发改动，不为开源而擅自发布安装包。分享撤销已由用户确认，当前引用清理完成，不再重复要求用户选择迁移或历史重写方案。
2. 提交/推送前明确发行授权。用户选择保留 main 自动发布，推送 main 的完整 CI 会继续触发正式候选和发布链，不能宣称这是单纯的公开源码操作。正式发行须按实际版本执行安装、签名和 R2 收尾。
3. 确认桌面仓库可见性变更后，启用私密漏洞报告及适用的 secret scanning/push protection；配置 main 与 ci-v*/v* 标签保护，核验 Actions 的外部贡献审批设置和维护者发布权限。
4. 从匿名访问和外部 PR 复核公开入口与测试边界。官网及两个 runtime fork 的状态按已确认范围维持。
