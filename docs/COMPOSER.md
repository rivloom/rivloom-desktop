# 简洁会话输入区

2026-09-10：按“简洁易用、不要堆叠”调整，基于已发布的 0.1.12；随 0.1.13 提交发行；公开状态以维护者本地验证记录的发布证据为准。

## 交互

- 默认只保留消息输入和一行工具栏：附件、目录、模型、Node 选择、会话设置、发送。1280px 合成界面中，空输入框从 284.19px 降至 128px。
- 移除常驻的附件配额说明和规划提示；配额与粘贴/拖入提示可在附件按钮悬停时查看，屏幕阅读器仍可读取配额及已选数量。
- 字数统计在接近上限（90%）或超限时显示。输入限制、超限禁止发送和原始草稿不变。
- 文件选中后显示紧凑列表，保留进度、错误、预览、移除和重试。长文件名省略显示，悬停可读全名；列表最多占 136px，更多文件可滚动查看。
- `@` 是统一 Node 入口，选择界面仍支持首选与锁定。选中的目标显示在工具栏，点击可修改模式或清除。
- 审批、执行位置和完成要求放入“会话设置”弹窗；关闭保留设置，Esc 返回入口焦点，目标清除后返回消息输入。设置弹窗不增加输入框高度。
- Enter 发送、Shift+Enter 换行和中文输入法确认维持原规则。普通 Task 的补充执行提醒、只读说明和错误提示继续显示。

范围仅为界面组织和样式；沿用连续会话、队列、草稿及附件路径。共享附件组件在非会话场景仍显示原有文字按钮、配额与数量。没有改动服务、原生应用、依赖、版本或官网。

## 验证

生产构建、类型与双语覆盖通过（1175 个 UI/native 键、585 个系统键）。版本一致性保持 0.1.12，测试分类审计通过，逻辑回归 373/373、零跳过。

独立 Edge 浏览器与本机回环合成 API 夹具通过 13 组 UI 检查，覆盖单行工具栏、设置与焦点、输入法/快捷键、Node 模式、输入上限、附件预览、粘贴/拖入事件、上传失败重试、刷新草稿、发送失败保留、同会话追加、双语 1280/900/420/360px、5 个长文件名和第 6 个文件的限额提示。截图已人工核对，零浏览器运行时错误。

粘贴/拖入使用浏览器构造的 ClipboardEvent/DragEvent；上传、草稿和发送使用隔离 API 响应。这验证本次界面重排的路径，不代表系统剪贴板、操作系统拖拽、原生 WebView 或真实模型的完整验收。未访问用户安装版、真实项目或模型，夹具与浏览器均已退出。

本地证据：`.data/verification/composer-20260910/` 下的 `baseline/`、`ui/ui-result.json`、界面截图、`build.log`、`logic.log`、`test-classification.log`；逻辑报告为 `test-results/ci/tests-logic-1789053427334-3218e7a7.json`。
