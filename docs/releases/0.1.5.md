Rivloom 0.1.5

- 新增启动时及每 6 小时自动检查官网更新；支持跳过单个版本、手动检查、下载进度、后台下载与取消。
- 更新安装前验证文件签名，等待未结束工作，安全退出后台并备份应用数据。安装中断后再次打开会显示恢复提示。
- 保留此前自动规划、任务图、跨机转交、并行执行和结果展示改进；本机任务并发不限，外来任务默认并发 3，可调 1–10。

首次使用：旧版 0.1.4 尚不具备更新功能，需要先手动安装本版，后续版本才可在应用内提示更新。Windows x64；不自动迁移旧 Preview 身份的数据。

安装更新前请处理尚未结束的任务；下载完成后若有工作未结束，应用会保留本次下载并提供稍后重试安装。Windows Authenticode 代码签名尚未配置。
