Rivloom 0.1.14

- 会话内容搜索：查找历史要求、回答、步骤旧尝试和待执行消息，查看命中片段并跳转到对应内容。
- 模型自由选择：连接厂商后，在会话中选择可用模型；默认模型只影响后续任务。
- 厂商接入：提供 API Key 和官方引擎当前支持的 OAuth 登录入口，按厂商显示授权方式与所需字段。
- 自定义 Provider：支持 OpenAI 兼容 Chat Completions / Responses、多个模型、本机地址和免 Key 服务，可编辑或移除配置。
- 连接测试覆盖已配置模型；真实测试需要确认，可能消耗所选服务额度。各厂商账号和 OAuth 可用性以实际授权结果为准。
- 修复清空搜索时短暂残留的命中状态。Windows x64。
