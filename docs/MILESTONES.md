# Rivloom 里程碑基线

更新日期：2026-09-09。

**2026-09-09 接手说明：** 自动规划、依赖任务图、并行汇合、资源查询、执行转交与来源并发已实现；本机并发不限，外来默认 3、可调 1–10。剩余开发、候选和待测状态见 [NEXT-SESSION](NEXT-SESSION.md) 与 [HANDOFF](HANDOFF.md) 顶部。下文较早阶段的“未获采纳/未实现”、旧包与测试数用于追溯；与 9 月 9 日记录冲突时，以最新实现与证据为准。本次只盘点，没有启动新功能或发布。

**2026-09-08 当前收尾：** 用户要求历史会话及配对机器侧栏可拖动后统一提交。宽度拖动、持久保存、键盘调整与恢复默认已完成，60 项界面、254 项逻辑、6 项真实隔离接口及类型/双语/构建通过。`3bf23807` 本地安装包的 Release/NSIS、源码/runtime 和解包校验通过。当前源码包含此前协作文件、待办通知、双语、品牌、性能和紧凑历史栏改进，按用户要求统一收尾；剩余实机、断网和性能/硬件边界按用户最新要求暂缓。当前通过项与暂停项以[实机记录](DESKTOP-ACCEPTANCE.md)为准，以下为历史阶段记录。

**2026-09-08 最新界面与候选：** 搜索旁筛选浮层、紧凑导航与品牌区完成；45 项 UI、20 项相关逻辑及类型/双语/构建通过。新包 `49b12819` 包含本轮界面和前两轮性能优化，Release/NSIS、源码/runtime 门禁和解包校验通过，仍待用户安装及实机回归。详见[侧栏记录](plans/2026-09-08-sidebar-compact.md)。下方 `43b47f27` 是本轮界面调整前的候选。

**2026-09-08 最新安装包状态：** 用户要求重新生成安装版，性能候选 `43b47f27` 已完成 Release/NSIS 和独立解包校验，包含两轮已验证的性能源码；安装文件与校验码见[底层性能记录](DESKTOP-NATIVE-PERFORMANCE.md)。尚未执行新包安装，用户自行安装；正式桌面/双机测试仍暂停，未签名或公开发布。以下“尚未生成/安装”属于本轮打包前状态。

**2026-09-08 最新性能状态：** 第二轮已完成异步资源采样、原生等待线程调整和后台任务索引查询，249 项逻辑、11 项协议、10 项 Rust、12 项完整服务检查及类型/双语/生产构建通过。源码基准及索引代价见[底层性能记录](DESKTOP-NATIVE-PERFORMANCE.md)。正式桌面与双机操作继续暂停，性能候选尚未生成/安装，真实拖动和原生交互仍待回归；不改变下方已通过或仍待测的实机边界。

**2026-09-07 最新验收状态：** 本地、双向远程、Brain、首次附件、结果回传、验收正文保留、队列暂停/恢复及正常退出后原队列恢复已有安装版实机证据。通知候选 `93b2431c` 的本机运行中、最小化恢复、退出后启动并定位原任务三种系统点击均已通过；另一台通知回归、真正断网、强制崩溃和传输中断仍待实机测试。用户要求暂缓正式桌面操作，先进行性能优化。首轮源码和隔离验证已完成：网络传输与输入脚本开销下降，236 项逻辑、7 项 Rust、每次 11 项 UI 检查通过；未生成/安装性能候选，原生窗口拖动仍未测量。见 [性能记录](DESKTOP-PERFORMANCE.md) 和 [待测清单](DESKTOP-ACCEPTANCE.md)，不得标记全部实机验收或公开发行完成。

**2026-09-06 追加状态：** 用户选择先开发待办/通知、连接诊断、附件与成果回传，之后统一真机测试。三项已开发并完成隔离工程检查；正式安装、真实模型、系统通知点击与双物理机仍待验收。新增源码尚未发布，当前公开包不包含这些变更。用法见 [协作文件说明](COLLABORATION-FILES.md)，决定见 [ADR-0007](adr/0007-attention-diagnostics-and-task-files.md)。其余 P1、M4、签名和更新不在此次范围。

**2026-09-06 公开发行基线：** M2/M5 的普通 Windows Release、官网上线、R2 匿名下载及发行后自动刷新官网已完成真实验证。M3.5 A–D 工程交付完成，用户及本轮双物理机验收仍待进行；下一阶段建议以 0.1.4 公开包完成真实使用验收。代码签名与客户端自动更新仍未启动。下面保留的旧候选包、待上线和历史测试数不代表当前公开发行状态，最新证据见 [验证报告](VERIFICATION.md) 顶部。

本文档记录当前产品路线、阶段状态和完成条件，是里程碑状态的首要依据。2026-09-05，M3.5 会话界面与四项 Node 协作 P0 已完成 A–D 工程交付、待用户验收；用户另已启动 M2/M5 官网、CI 与发行接口第一轮实施。更详细的历史实现和验证证据分别见 [实施进度](PROGRESS.md)、[实施计划](plans/2026-08-31-mvp-delivery.md)、[CI](CI.md) 和 [验证报告](VERIFICATION.md)。旧编号保留用于追溯，不代表暂停项会按编号自动启动。

## 当前共识

| 里程碑                        | 当前结论                                               | 接下来的处理                                                                                               |
| ----------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| M0 真实任务闭环               | 已确认完成                                             | 后续改动只做必要回归，不重新设计                                                                           |
| M1 模型接入                   | 已完成                                                 | 保持现有行为，必要时做真实任务回归                                                                         |
| M2 Windows 发行完善           | 0.1.4 普通发行及云端隔离安装验收已通过；阶段未全量完成 | 下一步真实用户安装/升级；签名、完整干净机/迁移矩阵与 Tauri updater 仍待实施与验证                          |
| M3.1 节点自发现               | 已确认完成                                             | 保持现有行为，必要时做回归                                                                                 |
| M3.2 配对、信任与加密连接     | 已确认完成                                             | 保持现有行为，必要时做回归                                                                                 |
| M3.3 跨节点任务协作           | MVP 验收完成                                           | 剩余回归和防火墙体验转入后续优化清单                                                                       |
| M3.4 自动 Brain 与共享 Worker | MVP 按当前确认范围结项；核心物理闭环通过               | 保留现有能力；纯 mDNS 92/93 单列待查，不代表全量通过                                                       |
| M3.5 会话式 Node 协作（当前） | A–D 工程交付完成；用户及双物理机验收待进行             | 服务 12/12、两崩溃窗口、Debug/Release 原生与最终二进制通过，新独立包已生成；全量 154/155，纯 mDNS 失败单列 |
| M4 ChatGPT 登录与其他模型     | 暂缓，未实现、未验证                                   | 按实际需求单独确认接入方式和验收；不作为 M3.5 前置                                                         |
| M5 商业内测交付               | 官网已上线，普通 Release、R2 下载和自动刷新官网已跑通  | 转入实际使用验收与内测反馈；不将分发成功视为全部产品验收或商业发行审查完成                                 |

## 当前工作顺序

1. 保留 M0、M1、M3.1–M3.4 已完成的当前 MVP 基线；M3.4 按 [ADR-0004](adr/0004-automatic-brain-masters-and-shared-workers.md) 结项，既有失败和未测边界继续单列，后续改动按影响回归。
2. **M3.5 会话式 Node 协作 A–D 工程交付完成**：路由/创建幂等/候选修复、持久队列/控制、回执/能力界面已经落地，隔离服务、真实 session 崩溃窗口、Debug/Release 原生与最终二进制闭环通过，独立预览包已生成。下一步检查预览、安排本轮双物理机回归并确认用户验收，不从 A 重做。
3. **M2/M5 官网及普通发行分发链路已完成验证**，不重做已经配置成功的 CI、R2 和官网同步。建议接下来使用当前 0.1.4 公开包完成真实安装及 M3.5 用户/双物理机验收。签名和客户端自动更新仍待单独推进；本轮已单独授权并开发待办、诊断和文件交付，其余 P1 与 M4 模型扩展仍未启动。
4. M3.3 的两真人与其余物理回归、防火墙体验以及纯 mDNS 专项保留为后续优化；本阶段触及的审批、停止、信任、准入和恢复行为仍须按 P0 计划做必要回归。
5. 保留 Brain/Task/Execution 归属、本地资源自治和固定 Master Host；不增加自动选主、人员治理、跨 Brain 迁移或共同任务。单 Brain 目标拆解/自动编排尚未获采纳，也不属于 M3.5。macOS 发行继续延后。超出已确认范围的变化再讨论并更新决策及验收。

## M3.5：会话式 Node 协作

当前停点：A–D 工程交付完成，用户验收和本轮双物理机回归待进行。完整服务 12/12、真实 session 两崩溃窗口、Debug 原生 12 条/Release 原生 8 条记录及最终二进制闭环通过；功能交付全量 154/155，唯一已有纯 mDNS 失败保留。当前检查使用文档修订 `docs1` 预览包，71,096,634 字节，NotSigned、未安装，完整路径/哈希见 UI-HANDOFF 和 VERIFICATION；原功能包及其 71,107,766 字节记录保留为历史。自有夹具均清理；此前 Escape 误触仅为历史。本轮 CI 原生候选尚待验收，不提前替换该交付基线。

**目标：** 用户在会话中选定执行 Node 后，可以准确知道任务是否送达、在哪排队、为什么等待；接收 Node 能控制真实等待顺序，任务按最终准入执行，双方看到一致结果。自动选择的候选规则与实际调度一致。

**范围和依据：** 已有会话式桌面为界面基线，新增四项 P0 为本阶段剩余工作。具体实现见 [P0 实施计划](plans/2026-09-05-node-collaboration-p0.md)、[ADR-0005](adr/0005-directed-node-queues-and-receipts.md) 和 [UI-HANDOFF](UI-HANDOFF.md)。P0 表示本阶段的优先交付范围，A–D 是内部实施检查点，均不另开新里程碑。

| 检查点                        | 当前状态                           | 交付与完成判断                                                                                                                            |
| ----------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 界面基线                      | 已实现，纳入本轮交付               | 会话布局、Node 名片、本地备注、`@Node` 直发；本轮 P0 另有独立验证，不用旧包替代新证据                                                     |
| A：路由与调度基础（Task 1–2） | 已实施，定向与服务验证通过         | 草稿以 Node ID 绑定目标；三处创建接口保存 requestID → 精确 Task ID；各 Brain 的提交候选与 Master 调度过滤共用规则；原 Task/Brain 重试不变 |
| B：持久队列与控制（Task 3–4） | 已实施，隔离服务验证通过           | SQLite Node 队列、owner-only 调序/暂缓/恢复/拒绝、单槽最终准入与恢复守卫已接入；混合来源实际顺序和 review/unknown 占槽已有本轮证据        |
| C：回执与能力界面（Task 5–6） | 已实施，Debug/Release 原生操作通过 | 23 项前端/HTTP 定向测试，Debug 草稿、队列/回执、断线和重启，以及 Release Windows IME 候选 Enter、键盘选目标、排位、统计刷新和等待文案通过 |
| D：验证与预览交付（Task 7）   | 工程交付完成，用户验收待进行       | 服务 12/12、两崩溃窗口、Debug 12/Release 8 条原生记录、最终二进制闭环及新独立 NSIS 通过；全量 154/155，纯 mDNS 失败与双物理机覆盖限制保留 |

本轮最终完整服务证据根为 `.data/verification/m35-p0-1788581065722-2746e0ac`，使用本机独立服务、官方 OpenCode 与确定性 loopback 模型：7 个业务 Task、7 个官方 session、0 工具，6 个 accepted，最后一个保留故障注入的 interrupted；普通成员读取范围、owner 队列权限与脱敏追加验证通过。此前 10/10 根及离线配对竞态失败根均保留。Debug 原生 12 条、Release 原生 8 条记录通过，分别保存 6/4 张截图，包含真实 Windows IME 候选 Enter 不发送与自有进程清理。此证据不等同于两台物理设备或真实模型编程验收，正式客户端和原物理验收数据没有改动。细项及未覆盖边界见 [VERIFICATION](VERIFICATION.md)。

### 完成条件

1. 普通忙碌或满槽时，`@Node` 仍在指定目标排队；草稿恢复、改名和重试不改变目标。离线或严重异常有真实原因，不能静默改派。
2. 显示顺序与服务端实际取队列顺序一致；暂停/恢复/调序/拒绝能持久保存并反馈给任务发起方。运行中沿用现有停止，可信自动收件和 AI 审批边界保持。
3. 创建重试、断线重连、回执乱序和接收端重启不产生重复活动 session；review、interrupted 和未知执行继续按原准入规则占槽，既有 Brain/Task/Execution 归属及历史保持。
4. 自动分配候选不一致的已知 queued 缺陷有复现和修复证据；无候选时给出原因，合法 Worker 出现后保留原 Task 继续，不以新建任务替代失败记录。
5. A–D 的通过、失败、未覆盖项和安装包信息记录完整，汇总结果供用户验收结项。旧 UI 包、旧 M3.4 物理证据或构建通过不能代替本阶段验证；两物理设备回归按计划另约，未完成前明确标注覆盖限制。

### 本阶段之外

用户于 2026-09-06 追加授权的待办、诊断、明确选择的附件与成果回传已开发，等待统一真机验收。上下文目录同步、离线首次投递信箱和独立结果凭证页等仍为候选后续工作，未指定新编号或完成日期。纯 mDNS 失败继续单列，不因 M3.5 结项变成通过。M2/M5 已按用户追加要求开始下方第一轮实施，M4 仍延后；当前预览包交付不等同于正式安装、签名发行或商业发布。

## M2/M5：官网、CI 与发行接口第一轮实施中

来源为用户提供的《官网分发与更新方案》讨论，随后用户要求开始第一轮实施。筛选与修正、技术依据、完整流程和验收矩阵统一见 [RELEASING](RELEASING.md)，架构取舍见 [ADR-0006](adr/0006-website-distribution-and-safe-updates.md)；各阶段验收仍按真实证据分别完成。

当前官网私有仓库 `rivloom/rivloom-website` 位于 `C:/project/rivloom-website`，首版 `f58049f` 已推送；Astro 9 页、234 项引用、发行目录测试 13/13 与本地 actionlint 通过。用户已授权 Cloudflare 仅访问官网仓库、推送/部署/绑定全球（含中国大陆）的 `rivloom.com`，并完成 GitHub 2FA。官网首轮云 CI 的锁文件问题已修复，推送和云验证重试、Cloudflare 连接及域名绑定正在推进；尚未确认线上站点成功，无公开下载。最新过程见 [官网交接](WEBSITE-HANDOFF.md)。

桌面四份 Windows 工作流、发行记录契约和 runtime 门槛已落地。前一轮版本/覆盖审计、TypeScript/Vite、CI 自检 9/9、逻辑 168/168、协议 11/11、引擎端口 2/2、UDP 1/1 与四组服务检查通过；纯 mDNS 0/1、0 跳过，该轮累计 182/183，后来 runtime 文件单独增至 16/16 通过。真实 prepare/gate 现已通过，核对 Node/OpenCode 哈希和版本、88 个运行依赖、267 个 Rust 依赖及 722 份 notices，证据 `.data/verification/ci-runtime-precommit.json`；Rust/Cargo 1.98.1 已安装并核对版本。本轮原生构建尚未执行，官网云 CI 与桌面纯 mDNS 的失败分别记录；updater、R2、签名和公开发行未实施，详见 [CI](CI.md)。

### M2：Windows 发行基础与客户端安全更新

**目标：** 用户安装和升级 Rivloom 时保留原身份、任务和数据，应用内更新可校验来源并妥善处理正在执行的工作。

| 检查点              | 待交付内容                                                                       | 完成条件                                                                                                            |
| ------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| M2-A 安装与签名基线 | 当前 Tauri Windows x64 NSIS、平台签名、随包运行时/许可清单、干净机验证           | 目标版本全新安装/覆盖升级/退出/卸载保留数据有证据；签名可核验，不承诺 SmartScreen 一定无提示                        |
| M2-B 安全更新       | 官方 Tauri updater、HTTPS 静态清单与更新签名、检查/下载/稍后安装、维护与迁移流程 | 旧 0.1.3 手动安装具备 updater 的版本，再完成一次真实应用内升级；错签名/损坏/网络失败不安装、不破坏现有数据          |
| M2-C 任务与兼容验证 | 新执行准入暂停、受控退出、原身份/队列/Task/Execution 恢复，协议与数据格式检查    | 运行/审批/验收/unknown、Master/Worker、新旧节点交叉、迁移中断与恢复均有结果；无重复 session、静默重派或强制数据降级 |

现有 `nodeProtocolVersion` 与 capability 继续使用，应用版本、节点协议、数据格式和随包组件版本分别管理。更新整个应用与已锁定 Node/OpenCode/SDK，不额外维护自研更新器或引擎独立自动更新。M2-A 的 runtime/许可门槛与候选构建基础已进入实施；安装签名、M2-B/C 更新与迁移验收尚未完成，不重做已有有效证据，也不把门槛夹具通过当作新包验证。

### M5：官网、产物分发与发布运营

**目标：** 用户能从可信入口下载适合自己的已验证版本，维护者能重复发布、停止故障版本投放并追溯每份产物。

| 检查点              | 待交付内容                                                             | 完成条件                                                                                       |
| ------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| M5-A 下载入口       | 静态官网、下载/文档/更新日志/隐私与反馈入口，受控 HTTPS 域名           | 只展示实际支持的 Windows x64；版本、大小、哈希、签名状态和限制与真实产物一致；目标地区下载实测 |
| M5-B 分发流水线     | 对象存储/CDN 或独立公开分发仓库，不可变版本目录，候选构建与频道提升    | 先上传并验证产物再更新清单；同版本不可换字节；私有源码凭据不进入客户端；Preview 与正式频道隔离 |
| M5-C 发布验收与应急 | 目标版本安装/更新/许可证据、发布清单、问题反馈、停发及更高版本修复演练 | 失败保留原频道；已装坏版本的用户有修复路径；不把回退清单等同于数据可降级或用户已自动回滚       |

受控未签名内测可以按既有约定明确交付限制；公开 stable 的推荐门槛是完成产品/安装器签名和 M2 安全升级验证。M4 新模型接入不构成前置，M3.5 独立预览不因此扩大验收范围。`rivloom.com` / Cloudflare 官网部署与绑定已获授权，连接和上线实施中；签名、分发预算及公开下载开放方式仍需落实。

动态更新 API、按人群灰度、nightly、企业版本策略、国内镜像和 macOS/Linux/ARM64 等保留为后续候选；不因分享对话列出这些能力就同时立项。首期优先静态分发与 stable/beta，不建设账户、License 或更新数据库。

## M3.4 已确认定义

- Brain 是网络自动形成的逻辑 master 和调度域，不是 Node、用户创建的工作区或单个 AI Agent。
- 网络中没有 Brain 时自动形成一个；空白新 Node 发现已有 Brain 时不永久创建自己的 Brain。两个已经形成的 Brain 相遇后保持独立，不自动拆分、合并或迁移任务。
- 一个 Brain 当前只有一个固定活动 Master Host；一个 Node 可以承载零个、一个或多个 Brain，也可以同时服务所有受信可达 Brain。
- 开启执行能力的 Node 自动成为所有受信可达 Brain 的共享 Worker，不需要逐 Brain 加入。Brain 选择候选 Node，Node 按本机全局并发、项目、模型和额度做最终准入。
- Project 是 Node 本地普通文件夹资源。同名目录不自动等价，不要求 Git，不扫描、快照或哈希。
- 模型、凭据、额度和模型选择完全由 Node 本地管理，Brain 不维护模型池。
- Worker 向 Brain 注册静态硬件档案和动态负载；Task 可携带可选硬件硬要求。
- Task 是稳定工作记录，Execution 是 Node 上的一次执行。Project Task 固定到 Project 的 Node；Portable Task 可在合格 Worker 间重新调度。
- 新 Task 由提交 Node 自动选择一个在线且拥有匹配 Worker 的 Brain；首次发送前固定归属，此后不跨 Brain 自动迁移。
- 个人与企业复用同一最小拓扑：Brain 持有 Task，Node 承载 Master/Worker，本地 Project、模型和算力由 Node 自治；企业角色、策略、审计和计费以后作为治理层叠加，不另造一套核心模型。

## M3.4 最小闭环与验收

1. 两个独立 Brain/master 与一个共享 Worker 同时在线；不出现创建或加入 Brain 的手工操作。
2. Worker 自动出现在两个 Brain 的目录中，并通过认证加密通道向两者报告静态硬件和更新中的动态负载。
3. 两个 Brain 各自创建并持有自己的 Task，按 Project、硬件要求、执行槽和负载自动选择 Worker。
4. 同一 Worker 对两个 Brain 的并发分配执行本机最终准入，同一槽位不会产生两个活动 Execution。
5. Project Task 不离开提供其 Project 的 Node；Portable Task 可以在其他满足条件的 Worker 上重试。
6. 一个 Master Host 离线时，其 Brain 暂停新调度；另一个 Brain 继续工作，不接管离线 Brain 的 Task。
7. Brain、Task 和 Execution 路由不串线，重复消息不重复执行；硬件报告不含路径、模型凭据、硬件序列号或 MAC。
8. Release WebView2 展示 Brain/master、共享 Worker、硬件/负载、调度选择与 Task 归属；真实执行仍使用官方未修改的 OpenCode。
9. 接受及状态回传容忍协议已有的有界机器时差；取消/过期和旧 Execution 隔离仍有效，不以两机毫秒级时钟同步作为运行前提。

只有上述自动化/独立实例验证和至少两台真实 Windows 设备的核心拓扑回归通过后，才能把 M3.4 标记为 MVP 完成。同一 Brain 的自动选主与脑裂、人员身份以及跨 Brain 共同任务不属于本次验收。

### 2026-09-03 核心物理验收结论

本节是当前结论；以下历史实现/失败记录不得重新解释为尚未进行物理竞争或需要重装。

| 验收片段                    | 当前事实与证据范围                                                                                                          |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 新Node自动接入已有Brain     | 5.20空白临时Brain自动撤回、接入5.33原Brain；90,641ms目录观察及单Brain文字闭环通过                                           |
| 两个既有独立Brain共享Worker | 真实5.20/5.33独立Master、同一受信Worker；目录/硬件/动态报告与90秒稳定检查通过                                               |
| 跨机执行和Project归属       | B跨机文字Task闭环通过；B正常离线期间A指定原Project执行、B同身份恢复及旧Task保持通过；不扩大为物理有排队Master重启           |
| 真实单槽竞争与原队列继续    | 2ebbcc36：B先执行、A在原Brain排队，review占槽；B验收后A原Task自动第二尝试，再分别completed/seq4，远端B最终回执07:04:36Z收齐 |
| 唯一执行与旧历史保留        | 最终5accepted/5官方session/7Execution/5Project/夹具2次/0工具、测试目录为空、原3条历史不变、槽1；被拒尝试不产生业务会话      |

本轮报告 `m34-physical-prepared-race-2ebbcc36.json` 已passed，完整事实见 [验证记录](VERIFICATION.md) 和 [物理清单](plans/m34-physical-acceptance.md)。**M3.4核心物理验收已完成；用户在收到结果及专项限制说明后回复“好，下一步吧”，2026-09-03据此按当前MVP范围结项。** 这不表示未测边界已通过，也不等于全量回归全绿。纯mDNS专项失败（全量92/93、单独复查同样失败）仍单列待查，不因正常mDNS+UDP路径物理通过而抹去。HA/脑裂、人员治理、跨Brain共同Task和既有M3.3后续优化不纳入本次扩范围。

### 结项后保留的问题和边界

- 纯mDNS独立发现测试重复失败、根因未知；正常产品mDNS+LAN UDP路径物理闭环已通过，两者分别记录，后续处理时间待定。
- 隔离测试Worker恢复预检当前只接纳accepted邀请，本轮保留一条合法declined尝试；若以后还要恢复同一测试根，需先验证助手兼容，不删除历史或换根。这不是已经复现的产品恢复故障。
- 物理有排队Master重启、异常断网、人员/企业治理、HA/脑裂及M3.3其余优化保持未覆盖/延后，不借结项改成通过。
- 结项不包含停止用户正式客户端、删除测试数据、重新安装、提交Git或对外发布；运行现场暂保留，后续清理单独明确目标。

### 2026-09-02 初始实现状态（历史）

已实现并自动验证：

- `brain-topology.json` 将 Brain/master 从设备密钥身份中分离；旧安装迁移为正式 Brain，新安装先形成 provisional Brain；遇到正式 Brain 会放弃空白 provisional，两个正式 Brain 不自动合并。
- 受信加密 Brain 目录周期性交换 hosted/registered Brain、队列深度和 Worker 报告。Worker 报告只含 Project ID/名称、硬件、负载和槽位；不含路径、AI 模型、Key、账号、MAC 或序列号。
- 提交 Node 先按正式/在线 Brain、队列和匹配 Worker 自动固定 Brain；远端或本机 Master 保存权威 Task，再按最新 Project、硬件、报告新鲜度、槽位和负载选择 Worker 并创建独立 Execution。Project/硬件约束随两跳幂等消息持久化。
- Worker 在节点级串行区内复核本机 Project、模型可用性、硬件报告和全局占用；Portable Task 使用本机隔离临时普通文件夹，不引入 Git、快照或哈希。
- 桌面已显示 Brain、Master Host、队列、共享 Worker、静态硬件、动态负载、项目资源、槽位和任务归属；不再让用户手选 Node/Brain。
- 普通 Worker 可以先自动固定一个 Brain，把稳定 Task 交给该 Brain 的远端 Master；Master 保存权威 Task、生成不同的 Execution ID、重新选择 Worker，并把分配/执行摘要回传提交节点。
- Worker 在全局原子准入区内完成最后一次策略、Project、模型、硬件和槽位复核后才回复接受；竞争失败会明确拒绝，不留下“已接受但未占槽”的悬空记录。
- Task 保存单调尝试号和 Execution 历史。明确拒绝、邀请过期或尚未接受时 Worker 离线会安全结束旧 Execution，在同一 Brain/Task 下换 Worker；旧 Execution ID 的迟到状态不能覆盖新尝试。已接受但是否启动未知时保持等待，不冒险重复执行。
- Windows 同机四独立数据目录测试已验证两个正式 Brain、两个共享 Worker、两跳调度、Task/Execution 分离、拒绝后换 Worker 和 Master 停止边界；停用发现刷新 20 秒后认证目录仍能保活。全量 32 项测试通过。
- 三个完整业务服务的 7 项检查通过，包括 90 秒稳定性与两个 Brain 竞争同一 Worker，确认只有一个业务 Task 和一个真实官方 OpenCode 会话；本地手动启动不能绕过该槽。模型为本机确定性夹具，不使用用户 Key 或额度。
- pending 离线、旧取消包丢失、迟到接受冲突拒绝、accepted 未知保持等待均经过真实网络故障注入。临时 Brain 撤回、可信显存检测和异步 GPU 采样也已加入回归。
- 最终 Release WebView2 三节点全互信拓扑稳定 90 秒；原生表单自动选择远端 Master，由共享 Worker 执行，提交端收到同一 Brain/Task 的结果并显示等待验收。分层限流解决发现流量挤占加密消息预算造成的周期性 429 掉线，认证与上限保留。
- 0.1.1 Release 和 NSIS 已重建；7 项真实安装器检查通过，包括全新安装、0.1.0 覆盖升级、身份/Project/Task/策略保留、重启与卸载保留数据；原安装元数据恢复单独核对通过。未更新用户实际安装，正式发行完善仍暂缓。

当时尚未达到完整验收（历史；以下物理执行/竞争/Project/正常Master离线现已按上节补齐）：

- 真实 5.20/5.33 已更新 0.1.1 并完成第三 Worker 配对，92.7 秒/91 次共享目录与槽位观察通过。首条 Task A 的接受回执被拒绝，尚未执行；0.1.2 已修复跨机器严格时间比较，新增 11 项/全量 43 项回归以及随包正负 5 秒完整服务各 8 项通过，新包已生成但用户实际安装未更新。按 [物理验收清单](plans/m34-physical-acceptance.md) 先退出正式 Master、再停自有测试助手并升级，不取消/重发来规避。
- 物理 Task 执行、单槽竞争、Project 固定和 Master 停止仍待验证；既有同机及 0.1.1 安装测试不能代替新包或上述多机验收。

## M1 真实验证结果

1. 用户在本机“模型与额度”页面保存 DeepSeek Key，界面不回显完整 Key；真实连接测试通过且没有自动重试。
2. `deepseek/deepseek-v4-flash` 在没有 `.git` 的专用普通文件夹执行固定 slugify 编程任务。
3. 操作者拒绝一次计划外目录列举，补充约束后继续；随后分别批准预期文件编辑和 `node --test slugify.test.mjs`。
4. 独立核对确认只存在两个预期文件、测试文件未改、1 项测试通过且 0 项失败，任务最终完成验收。
5. OpenCode 官方 `session.diff` 返回空数组；界面如实显示无会话差异，没有使用 Rivloom 文件快照或内容哈希兜底。
6. 报告只记录模型标识、会话 ID、操作分类与结果，不记录 Key、完整回复或用户项目内容。

项目文件夹不要求 Git。Rivloom 不为任务建立文件快照或内容哈希；若 OpenCode 官方会话接口返回差异则展示，否则由验收人直接检查本地文件和测试记录。

## M3.3 当前已知事实

- 可信节点之间已经可以发送加密协作任务。
- 受信节点发来的任务会自动接收；本机可关闭执行能力或预设项目和模型。
- AI 操作审批已支持“请求批准 / 帮我批准 / 允许任何操作”，并通过官方 OpenCode 会话权限接口传入。
- 发起端已经能收到有序执行状态和最终摘要。
- 发起端已经能在加密任务通道上批准/拒绝具体操作、回答 AI 提问、停止远端任务、补充要求并继续同一 OpenCode 会话，以及提交最终验收；控制会校验设备信任、任务路由、唯一控制 ID 和执行序号。
- 审批和提问快照不含本机项目 ID、模型、凭据或 metadata，项目根目录若出现则显示为 `<project>`。
- 发起端可查看限长、脱敏的 OpenCode 官方会话差异；官方接口无差异时明确提示由执行机参与者本地核对，不扫描或哈希文件夹。
- 安全通道已覆盖单边会话丢失：较大 Node ID 发送签名恢复请求，由确定的较小 Node ID 重建通道，避免双方并发握手。
- 2026-09-02，Win11 `192.168.5.20` 与当前开发机 `192.168.5.33` 已用真实 DeepSeek 和普通文件夹完成单人双机闭环：可信任务自动接收、远程一次性批准文件写入、生成文件、补充要求后续跑同一 OpenCode 会话、本地核对结果和归属端最终验收均通过。
- 本次发现 `5.33` 上随包 `node.exe` 的 TCP/UDP 入站规则虽已启用但 `Action=Block`；改为 Private Profile 的 Allow 后，`5.20 → 5.33` TCP 探测和自动发现恢复。安装器尚未可靠创建、检测或修复规则；该项已延后优化，不阻塞 M3.4。
- 产品功能已形成“发送 → 自动执行 → 人工介入 → 补充 → 查看结果 → 验收”闭环；单人双机核心体验已验收，M3.3 的 MVP 范围完成。
- 远程停止、另外两种审批、撤销信任、异常断网恢复、两真人角色回归及防火墙安装体验保留为明确的后续优化，不作为当前推进阻塞项。
- 当前“单个可复用项目和模型”的实现是 M3.3 已验收切片；它不预先决定 M3.4 的 Brain 与资源模型。

## 暂停与延后项

- 同一 Brain 的状态复制、自动选主、Master 迁移、任期 fencing 和脑裂处理；不影响当前已实现的 Worker Execution ID fencing。
- 人员身份、一人多节点、一节点多人、企业角色与跨 Brain 共同任务。
- Brain 按规模或负载自动拆分、创建或合并。
- 不用旧的 30 分钟准备租约流程。
- ChatGPT 登录与更多模型供应商继续延后；M2/M5 官网、CI 与发行接口第一轮已启动，自动更新、正式发行签名和 R2 分发尚未实施。
- 不把尚未完成的两真人、停止/自动审批/撤销及防火墙回归描述成已验证；它们已记录为后续优化。
