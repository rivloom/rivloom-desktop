> 2026-09-10：0.1.9（6a0effb5）和官网（3f7fef09）已公开验证。用户进一步要求目录、会话、文件三类右键菜单的全部操作，本轮实现 0.1.10：统一鼠标/键盘/更多按钮入口；目录别名、复制路径、打开文件夹；会话用户级持久重命名、目录内置顶和既有回收站；文件默认应用打开、定位、另存为、复制路径。名称不改写执行请求，永久删除清理偏好。新原生命令已注册权限并经过交叉检查。359 项逻辑、26 项原生、10 项隔离服务和 16 项生产 UI 检查通过；沿用既有签名密钥，实体安装和真实模型测试继续暂停。发行仍须通过本提交云端安装与公开验证。

> 2026-09-10：0.1.5 已公开发行（3864e691），官网与签名更新通道已验证。本轮按用户要求实现会话删除/回收站、三个月保留与自动清理、恢复/永久删除/清空、工作目录分组，并升为 0.1.6。已通过隔离服务生命周期检查，新增防重放与共享附件测试；发布须等待本提交的全部 CI、独立安装验收和签名清单验证。继续保留原更新密钥，不改动实体安装与真实模型环境。产品说明见 [会话历史](CONVERSATION-HISTORY.md)，发行说明见 [0.1.6](releases/0.1.6.md)。以下旧候选与暂停约定为带日期的历史记录。

> 2026-09-10 发布授权：用户要求推送此前全部改动并上线 0.1.5，另明确允许配置应用内更新。此前“不提交、不推送、不发布”的限制已被本次授权替代。更新私钥已安全保存为私有仓库 Actions Secret，RIVLOOM_UPDATES_ENABLED 已开启；私钥不进入 Git 或日志。上线须以新提交的全部 CI、Release、R2 和签名清单校验成功为准。首次 CI 发现两处旧并发测试假设和退出期间状态查询竞争；已修复，三项官方引擎隔离服务回归通过。实体安装与真实模型测试仍由用户安排。

# Rivloom 新会话接手清单

更新：2026-09-09。用户随后明确要求实现启动和定期检查更新；0.1.5 已完成该功能及本地验证，详见 [应用内更新](DESKTOP-UPDATES.md)。其他未选开发项和暂停测试保持原范围。

**当前范围：** 本轮只新增应用内更新与所需验证/打包；不恢复真实模型、双物理机或性能补测，不自动提交、推送或发布。此前模型与软件问题的区分继续有效。

## 1. 从哪里继续

- 工作区：`C:\project\rivloom-opencode`。当前包含大量已完成但未提交的变更和新增文件，不要清理、重置或从旧提交重新实现。
- 先读本文，再读 [HANDOFF](HANDOFF.md) 顶部、[来源并发方案](plans/2026-09-09-origin-concurrency.md) 和所选工作对应的代码。
- 最新更新源码为 `0.1.5`，本地构建目录 `.data/verification/desktop-updater-candidate-20260909/`。此前 `a95315c1` 是保留的 0.1.4 候选，其 digest 不代表当前源码。自动规划、资源发现、转交、并行分配、结果展示、队列、审批和目录修复保留。
- 本机任务不设固定并发上限；外来任务共用独立名额，默认 3，可调 1–10。同一目录允许多个会话。不要恢复成单机固定一个槽位。
- 正式应用身份仍为 Rivloom / `com.rivloom.desktop`。用户此前已选择普通 Release 和官网去 Preview；小范围试用建议不等于授权改成 Preview 应用或更改发布频道。

## 2. 已实现，不要列为待开发

真实任务闭环、模型接入、配对与加密通信、Brain/Worker、持久队列与回执、暂停/恢复/停止、执行审批、附件与成果回传、通知、连接诊断、中英文、可调整侧栏都已有实现。

9 月 9 日还已实现：选定工作目录后的资源登记与 AI 查询；新请求的真实只读自动规划；依赖任务图、并行分支与汇合；`@Node` 首选和 `@@Node` 锁定；保存文件与上下文后的执行转交；规划失败后重新规划；未开始步骤编辑；结果优先、过程折叠、交付文件蓝色链接。

官网、Windows CI 候选、普通 GitHub Release、R2 匿名下载和发行后官网刷新已在 9 月 6 日跑通。当前线上状态未在本次重新核查，下一次发布时复核即可，不需要另造一套分发链路。见 [RELEASING](RELEASING.md)、[CI](CI.md)。

旧路线图中“自动编排未获采纳”“任务图只有概念”“单机并发仍为 1”等表述已被后续实现取代；保留这些历史记录不代表当前缺失。

## 3. 仍值得开发的内容

以下区分已有长期计划与本次建议。用户本次要求盘点，并未一次性批准整张清单。

| 优先级 | 内容与现状 | 建议的最小交付与验收 |
| --- | --- | --- |
| P1 候选，产品增强建议 | **结果自动校验**：执行成功会直接完成，但尚无通用业务验收层。此前要求 2 秒的视频实际仅 1.7 秒，说明模型完成状态不足以判定交付符合要求；这本身不证明应用执行有缺陷，也不使该建议成为必修软件问题。 | 若后续决定实施，先针对明确、可测的要求保存校验项，例如文件存在、格式、数量、视频轨时长；展示实际测量结果和未达标项。无法客观核验时显示未验证。保留用户已选择的自动完成体验，不重新加入每次人工验收弹窗。验收包含“模型声称完成但产物不符合要求”的反例。 |
| P1，建议 | **失败步骤单独重跑**：当前工作流控制只有暂停、恢复、停止、重新规划；业务步骤失败后缺少完整的局部重试和下游结果失效规则。 | 先定义用户可选择重跑哪个步骤、哪些下游需重新执行，以及保留哪些成功分支。保存尝试历史与产物版本；仍在执行或停止结果未知时阻止重复运行。用分叉—汇合、重复点击、重启和旧产物引用验证。 |
| P1，M2 剩余事项 | **更新上线、迁移与恢复验收**：0.1.5 已实现检查/跳过/下载/验签、维护栅栏、应用备份和安装恢复提示。 | 配置匹配的 Actions 私钥并启用更新频道，完成旧正式版升级与安装中断实测。仍缺完整有序迁移、用户恢复工具与 Windows Authenticode；不能把客户端代码通过等同公网升级已通过。见 [更新说明](DESKTOP-UPDATES.md)。 |
| P2，既有 P1 延后项 | **首次离线投递**：已创建的投递有持久记录和重试，首次目标离线仍明确失败并保留草稿。 | 增加本机持久待发送记录，显示未送达、取消和过期；重连后按原 requestID 投递一次。锁定节点不自动换机，接收方仍执行自身权限与队列策略。重启、撤信、取消后重连和回执丢失均不重复创建任务。 |
| P2，建议 | **使用后的维护工具**：已有连接诊断，缺少面向反馈的脱敏诊断包；日志、历史和传输副本的保留/清理，以及用户可操作的备份恢复仍待完善。 | 先提供用户主动导出的诊断包与内容预览，排除凭据及默认不带任务正文；清理提供范围/体积预览，保护活动任务及原始项目文件。备份恢复与上一项迁移机制共用，避免两套恢复逻辑。 |

更新功能本轮已选并实现；后续可在“结果自动校验”“失败步骤单独重跑”与“更新上线及恢复验收”之间按实际目标选择小阶段。结果自动校验属于产品增强，不因本次模型表现而自动列为必须修复项；本次只记录，开发范围和顺序留到后续决定。

相关代码与依据：

- 工作流与结果：[workflow-service.ts](../server/workflow-service.ts)、[workflow-runtime.ts](../server/workflow-runtime.ts)、[workflow-files.ts](../server/workflow-files.ts)、[workflow-view.tsx](../src/workflow-view.tsx)、[workflow-results.ts](../src/workflow-results.ts)。
- 更新与数据：[RELEASING](RELEASING.md)、[store.ts](../server/store.ts)、[Tauri 入口](../src-tauri/src/main.rs)。不能只备份 SQLite，Node 身份、信任及其他 JSON 状态也要覆盖。
- 离线投递：[ADR-0005](adr/0005-directed-node-queues-and-receipts.md)、[node-network.ts](../server/node-network.ts)、[node-queue.ts](../server/node-queue.ts)。
- 维护工具：[node-diagnostics.tsx](../src/node-diagnostics.tsx)、[SECURITY](../SECURITY.md)。

## 4. 待验证，与待开发分开

**业务失败的归因澄清（2026-09-09 会话结束前确认）：** 此前真实协作测试发现的软件问题，包括能力信息过期导致选机错误、文件折返冲突、转交执行上下文不清晰，已经修复并通过针对性检查。剩余观察更偏向所测模型的指令遵循与生成质量：Nemotron 在明确禁止命令时仍请求 `ls`，另有要求 2 秒而视频轨仅 1.7 秒的产物。后者支持生成质量问题的判断，但没有另一模型的完整对照，不能断言剩余失败全部来自模型。

准确状态是：**上述已发现的软件缺陷已修复；所测模型尚未稳定完成全部业务场景，最新候选的完整真实模型正向验收仍待完成。** “完整业务验收未通过”不等于已经确认软件仍有缺陷，也不表示软件可以保证任意模型的输出质量；历史失败保留，不改成通过。以后若恢复验证，应结合模型输出、应用实际派发/传递记录及产物测量分别定位问题，再决定是否修改软件。

1. **最新候选的真实双机、真实模型业务正向验收**尚未完整通过。早期包补充说明后成功不能算首次成功；后续真实 Nemotron 检查仍有未通过项。若用户后续恢复此项，再验证原始需求不补充纠正时的规划、正向转交和完整产物，必要时做另一模型对照。该项当前属于待验证，不直接登记为软件缺陷。
2. **真实网络故障**：纯 mDNS 在新旧冻结源码都出现超时；物理断网、传输中断、强制崩溃及恢复的剩余边界仍需验证，不能由普通连接或受控重启结果代替。
3. **长时间实际负载和桌面流畅度**：23 个隔离官方会话同时运行已验证准入正确性，但不代表 23 个真实模型/高负载业务能稳定运行；长历史、硬件采样、输入法及原生拖动需要实际测量。
4. **最新包安装与升级**：由用户自行安装。此前暂停的实机和性能补测继续暂停，除非用户恢复该范围；新会话不要自行操作已安装应用或消耗外部模型额度。

记录：[DESKTOP-ACCEPTANCE](DESKTOP-ACCEPTANCE.md)、[真实协作复测](plans/2026-09-09-collaboration-fix-retest.md)、[底层性能](DESKTOP-NATIVE-PERFORMANCE.md)。这些是按影响安排的验证工作，发现可复现缺陷后再转成修复项。

## 5. 若下一步选择发布

当前源码已同步为 `0.1.5`，包含更新功能及此前所有未提交成果；不能覆盖公开的 `0.1.4`。发布前使用既有 CI 跑目标版本门禁、核对变更说明、Release/R2/官网与签名更新频道。按明确目标决定验收范围；已知失败和未测项继续如实保留。签名与自动更新是否纳入该次发布，需要结合目标落实。

本次未安装、进行 Windows 代码签名、提交、推送或发布。已生成本地受限 updater 密钥并使用独立测试密钥验证官方签名；生产私钥不得遗失或重新生成。

## 6. 更新验证与历史候选证据

0.1.5 更新测试：`.data/verification/desktop-updater-20260909/`，本地安装候选：`.data/verification/desktop-updater-candidate-20260909/`，结果以实际报告为准。已通过 341 逻辑、13 协议、2 引擎、65 CI helper、18 Rust、5 隔离更新服务和 7 生产 UI 场景。

下列是保留的旧 `a95315c1` 候选记录：

- 安装包：`.data/verification/origin-concurrency-candidate-20260909/delivery/Rivloom_0.1.4_origin_concurrency_20260909_a95315c1_x64-setup.exe`。
- 大小：79,958,883 字节；SHA-256：`d4eb1c48ed09044ae40f311919c7516093abfe8459a4f50319850667480b5349`。
- 冻结源码：1,151 个文件；digest：`a95315c15be3576a88df85f2f6f1effc69361f19f74495868416004253c5526a`，基于未提交工作区。
- 候选记录：`.data/verification/origin-concurrency-candidate-20260909/delivery-result.json` 与 `source-context.json`；测试证据：`.data/verification/origin-concurrency-20260909/`。
- 已通过：336 项逻辑、13 项协议、16 项官方工作流、12 项 Node P0、5 项来源并发和 34 项生产 UI 检查；官方引擎隔离环境验证了同目录 13 本机 + 10 外来会话并发。模型响应来自本地夹具，UI 为合成场景。
- Release/NSIS、7,665 个 runtime 文件解包一致性及安装脚本静态对比通过。候选未安装、未签名、未发布。
- 本交接文件与 HANDOFF/MILESTONES 顶部提示在候选冻结后补写；没有改动冻结快照、安装包或产品代码。不要把当前整棵工作区称为仍具有上述源码 digest。

## 7. 暂不自动扩大的范围

ChatGPT 登录及更多模型入口（M4）、macOS/Linux/ARM64、人员与角色治理、授权计费、跨 Brain 共同任务、自动选主/接管、Master 迁移，仍需独立需求与范围决定。普通项目目录不要求 Git 仓库；现有本地资源自治、任务归属与信任边界保持。

## 给新会话的开场白

> 继续 Rivloom，工作区是 C:\project\rivloom-opencode。先读 docs/NEXT-SESSION.md 和 docs/HANDOFF.md 顶部，核对当前代码后，帮我确定下一个最值得开发的小阶段。当前源码为 0.1.5，已新增应用内更新；a95315c1 是保留的旧候选。已有很多未提交成果，不要重做旧路线图中已实现的功能。本机并发不限，外来默认 3、可调 1–10；安装由我完成，暂停的实机测试先保持暂停。


## 0.1.5 本地安装候选（本轮最终记录）

- 安装包：[Rivloom 0.1.5 更新功能候选](../.data/verification/desktop-updater-candidate-20260909/delivery/Rivloom_0.1.5_desktop_updater_20260909_cd898d08_x64-setup.exe)。
- 大小 81,093,072 字节；SHA-256：`964b44b6cf6ea73c57dd2179973bc675265ff5af4d481e03b78b15ebdbe7bd45`。
- 冻结源码 1251 个文件，digest：`cd898d081937a4531056a097834c03d9959e12c08053b8e66b8e6d0c1597563e`；保留已有未提交成果，无文件删除。
- Release/NSIS 构建、构建前后源码/runtime 一致性、7,736 个运行时文件解包比对和 NSIS 安装/卸载控制流静态对比通过。
- 本地包未安装、未进行 Windows 代码签名、未发布；不替代真实升级测试或云端干净候选。旧版先手动接入，签名频道按 [应用内更新说明](DESKTOP-UPDATES.md) 后续配置。
- 本段及 HANDOFF 顶部指向最终包的文字在冻结后补写，仅文档变更；不改变冻结快照与安装包，不把当前整棵工作区重新声明为该 digest。
