# Rivloom 新会话接手清单

**2026-09-15 · 独立维护政策：** 用户决定自行开发，不接受外部 PR。桌面与两个 runtime fork 的 PR 创建权限已设为 `collaborators_only`，源码保持公开与原许可；桌面 Issue 和私密漏洞报告继续开放。README、CONTRIBUTING、维护者 PR 模板与官网双语文案同步此政策。外部 fork PR 验收已从当前待办移除，不再要求用户安排该验收；以下“外部 PR 尚待验收”属于此前政策的历史记录。维护者现有权限、保护规则及 main 自动发布机制保留，桌面此次仅以 `[skip ci]` 提交文档，不发行新安装包。

**2026-09-15 · 开源收尾：** 用户已授权整理提交开源文档，并开启两个公开 runtime fork 的密钥扫描与推送保护。两个 fork 的两项设置均经 API 回读确认为 enabled，默认分支源码和 Actions token 权限未变。README 的官网私有仓库行已移除、免费 Star 历史图已加入；贡献指南、ADR 0011 与官网交接已同步。此次为纯文档维护，沿用 `[skip ci]` 提交方式，正式安装包仍为已验收的 0.1.15，不重复发行或执行 R2 清理。真实外部 fork PR 验收尚未执行，自有 runtime 发行物尚未接入。详情见 [开源准备记录](OPEN-SOURCE-READINESS.md) 顶部。

**下一步：** Skills/Wiki 的实现、提交与 0.1.15 发布已经完成，不再重复列为待开发或待授权。记忆默认本机、手动分享给 Brain；整理当前为分类索引及完全重复标记，独立 AI 后台语义重写未做。rivloom-desktop 已按用户授权以 Apache-2.0 公开，匿名访问、私密漏洞报告、秘密扫描/推送保护、main 和发布标签保护已核验；外部贡献的真实 PR 运行尚未验收。官网继续私有，合作邮箱暂缓。真实账号、额度、用户安装版/项目、双物理机和性能测试继续沿用既有边界。

**当前正式版本：0.1.15（2026-09-15，已发布并完成收尾）。** 共享 Skills 与渐进式 Wiki 记忆已发行。桌面安装包源码 `97ab7937b272278f876070de66ca3acc745dbe21`，官网源码 `29a8f27e1b6552976859346f0d522234b571f9f7`，构建 `v0.1.15-97ab7937b272-10387819109`。目标提交完整 CI、原生构建、隔离安装/启动/重启/卸载、Release、R2 与签名更新均通过；匿名完整下载、原更新公钥及官网实际部署核验通过。R2 41 个对象：保留 41、暂缓 0、删除 0，当前引用复核通过。下方“未提交/待重跑/发布准备/0.1.14”均为历史阶段，不覆盖本段。

**2026-09-15 · 0.1.15 已提交并通过安装验收，发布被 GitHub 计费拦截：** 正式源码 `97ab7937b272278f876070de66ca3acc745dbe21` 已推送；三组 CI、原生测试、NSIS、runtime 校验、隔离安装/启动/重启/卸载全部通过。运行 [34946471004](https://github.com/rivloom/rivloom-desktop/actions/runs/34946471004) 的发布 job 尚未启动即被 GitHub 拒绝，注释指出账户付款失败或 Actions 支出限额。候选 artifact `10387819109` 已保存。用户需要处理组织 Billing & plans，恢复后优先重跑该运行的失败任务，复用已验收候选。Release、R2 上传、签名更新、官网部署和本版 R2 保留检查均未完成；公开下载与原签名清单已核对仍为 0.1.14。官网 5 个配套文件已准备但未提交部署。下方“待重跑 CI/正式发布完成”不覆盖本段；本次提交升级授权继续有效，不需要重复确认。

**2026-09-15 · 0.1.15 CI 修复待重跑：** 已按授权提交并推送 `6e43f3b`。首轮服务 CI 暴露隔离目录依赖定位问题，候选门禁正确阻止发行；本地修复同时解决异步协作回执受反向消息序列推进影响的问题。416 项逻辑、13 项协议、69 项 CI 自检、CI 原入口三节点知识库 6 组及连续 24 次远端读取通过；下一步推送修复，等待同提交完整发布链和收尾。正式入口仍为 0.1.14，详情见 [验证记录](VERIFICATION.md) 顶部。下方“接下来首次提交”与“未获授权”均为历史阶段。

**2026-09-15 · 0.1.15 发布进行中：** 用户已明确授权补丁版本加一、提交并升级。发布范围为共享 Skills、渐进式 Wiki 记忆及已完成的开源准备修复。版本已统一为 0.1.15；接下来提交并推送 main，执行同提交完整 CI、隔离安装、公开下载与原密钥签名核验、官网部署核对和 R2 保留检查。尚未完成的步骤不得记为发布成功。仓库公开与远端保护设置另行处理；真实账号/额度、用户安装版与项目数据仍不作为测试夹具。下方“提交/发行待授权”是之前阶段的记录，本次授权已经满足。

**开源准备（2026-09-15，未提交/推送）：** Apache-2.0 / Copyright 2026 Rivloom contributors、官网继续私有、两个公开 runtime fork 仅查配置、公开时启用 GitHub 私密漏洞报告、保留 main 自动发布等决定不变。许可/随包门禁、贡献文档和发布来源保护已修复，414 项逻辑、68 项 CI 自检与独立源码构建通过。全部 399 个 artifacts、15 份校验文件及 15 个 Release 安装器核验完成；解包及当前/历史二进制可读字符串扫描无新增真实凭据发现。**用户已明确确认在账号内删除 ChatGPT 分享，当前四处链接引用已清理；保留原仓库、CI/CD 和 Git 历史。** 匿名访问仍受连接故障限制，没有记为独立验证通过；37 个含链接提交及 15 个标签保持原样。不要再要求用户重复确认撤销或选择迁移/重写方案。详见 [开源准备](OPEN-SOURCE-READINESS.md)。下一步审查公开源码范围与提交/发布安排；公开、提交/推送 main、改远端权限仍须用户确认。下方 Skills/Wiki 已完成状态继续有效。

**最新开发（2026-09-14，工作区未发布）：** 共享 Skills 与渐进式 Wiki 记忆第一版已实现。用户明确选择“记忆默认本机，手动分享给 Brain”。侧栏“技能与记忆”支持登记/分享状态、Brain 中继目录、最新版按需读取、分类与项目范围、历史、本机主说明和目录说明。413 项逻辑、13 项协议、3 项引擎、6 组三节点服务、65 项 CI 自检及 UI/双语/生产构建通过，8 次模型请求全部为本机模拟。未提交、推送、打包、安装或发布，正式版仍为 0.1.14。

下一步根据用户验收反馈修改，不重做已完成项。`npm run preview:knowledge` 启动合成 UI，`npm run test:knowledge` 复测隔离三节点。阅读 [知识库说明](KNOWLEDGE-LIBRARY.md)、[ADR 0010](adr/0010-shared-skills-memory.md) 和 [验证记录](VERIFICATION.md) 顶部。自动整理现为分类索引及完全重复标记，独立 AI 定时重写和语义合并未做；真实模型和双物理机效果未验收。真实账号由用户操作，真实额度调用先确认。下方旧记录不覆盖本段。

**新会话下一步：** 用户计划重新开会话验收本版。会话内容搜索与命中定位、模型自由选择、厂商 OAuth/API Key、自定义 Provider（Chat Completions / Responses）都已实现并随 0.1.14 发布，不要重复列为缺失功能。先读本页顶部、[模型接入](MODEL-ACCESS.md)、[搜索说明](CONVERSATION-SEARCH.md)和[缓存研究](PROMPT-CACHING.md)，按用户反馈继续。缓存部分只完成研究，没有缓存命中率优化或实测承诺。真实厂商登录由用户操作，真实额度、用户安装版/项目、物理机和性能验收继续沿用原授权边界。

**当前正式版本：0.1.14（2026-09-14，已发布并完成收尾）。** 桌面安装包源码 `0f8d8b5b68f8df6357a8deaec8ead7be29d39465`，官网源码 `269d9ead1e3e988f08a294f748ca9dd1514dd5b4`，构建 `v0.1.14-0f8d8b5b68f8-10337835771`。目标提交三组 Windows CI、原生构建、隔离安装/启动/重启/卸载、Release、R2 与签名更新全部通过；匿名完整下载及原更新公钥核验通过，中英文官网已实际部署。R2 38 个对象逐项核对：保留 38、暂缓 0、删除 0，当前引用复核通过。下方“发布准备/未发行/0.1.13”均为历史记录，不覆盖本段状态。

**2026-09-14 · 0.1.14 发布准备：** 用户已授权发布当前实现。本版包含会话内容搜索与定位、模型自由选择、厂商 OAuth/API Key、自定义 OpenAI 兼容 Provider（Chat Completions / Responses）。本地 403 项逻辑、3 项官方引擎、12 组模型服务、10 组模型 UI 和 14 组搜索回归通过。正式发布须完成目标提交完整 CI、隔离安装、公开下载与原密钥签名核验、官网实际部署和 R2 保留检查。真实 OAuth 登录与真实额度调用仍留待用户验收。下面“未提交/未发布/0.1.13”均为历史状态，以本页顶部为准。

**最新开发（2026-09-14，待用户验收）：** 已在会话搜索之后完成模型自由选择、厂商 OAuth、通用 API Key 和自定义 OpenAI 兼容 Provider（Chat Completions / Responses），支持编辑、移除、免 Key、本机地址与所有已配置模型的连接测试。403 项逻辑、3 项官方引擎、12 组模型服务、10 组模型 UI 和 14 组搜索回归通过；真实厂商 OAuth 及真实模型仍待用户手动验收，未使用真实额度。见 [模型接入](MODEL-ACCESS.md)、[缓存研究](PROMPT-CACHING.md) 与 [验证记录](VERIFICATION.md)。本机可双击 `.data/verification/model-access-20260913/打开模型接入验收.cmd` 查看合成预览。正式版仍为 0.1.13，本轮未提交、推送或发布，保留此前搜索与 R2 文档改动。

**最新开发（2026-09-13，待用户验收）：** 按用户“今晚开发、明天验收”的授权完成会话内容搜索与命中定位，覆盖历史轮次、需求/回答、步骤旧尝试及待执行消息。当前工作区保留此前 R2 文档改动；本轮未提交、推送或发布，正式版仍为已发布的 0.1.13（`14ee393`）。393 项逻辑、14 组搜索 UI、13 组输入框 UI、类型/双语/生产构建通过。阅读 [会话搜索与验收步骤](CONVERSATION-SEARCH.md)；本机双击 `.data/verification/conversation-search-20260913/打开搜索验收.cmd` 可打开冻结构建的合成预览。预览不调用模型、不访问安装版数据；真实模型/物理机/原生性能测试仍暂缓。下面旧版本和“下一批尚未选定”是历史记录；会话搜索与失败步骤重试均已完成，不应再次作为缺失功能开发。

**最新状态（2026-09-11）：** 0.1.13 已发布，桌面源码 `14ee3931b3e266b50717684d8510f0500c4110c9`，官网源码 `8f0a1334d3753d83281ea7ae57f0fce992673d96`；完整发布和公开核验通过。下方“发布准备/未发行”与 0.1.12 基线保留为历史记录。用户新增确认了 [R2 保留策略](R2-RETENTION.md)，已经纳入根目录 `AGENTS.md`：每次发布必须检查并记录最近 3 个版本、近 30 天与当前引用的并集保护；自动清理代码尚未实现，不能跳过收尾检查或误称自动清理已上线。本次文档落地没有删除 R2 对象。

**2026-09-11 · 0.1.13 发布准备：** 用户已授权提交、推送和发布更新。本版包含简洁输入框、失败步骤重试、结果分层、远端文件按需取回与紧凑排版；本地验证通过，正式发布待目标提交完整 CI、隔离安装、公开下载和签名核验。沿用既有更新密钥与发布流程。下面“未提交/未发布”的开发段落保留为当时记录，不能覆盖本段状态。

**最新开发（2026-09-11，未发行）：** 已完成用户选定的失败业务步骤单独重试、回答/文件/过程分层、远端成果按需取回和更紧凑的对话排版，保留下面已完成的简洁输入框。逻辑 380/380、协议 13/13、官方引擎双隔离服务、重启/离线/仅元数据续聊及 18 组 UI 检查通过。说明见 [恢复与成果](WORKFLOW-RECOVERY-RESULTS.md)，证据见 [VERIFICATION](VERIFICATION.md) 顶部。代码仍在当前工作区，未提交、推送、打包或发布；版本仍为 0.1.12。后文“下一项尚未选定/仅文档”属于此前发布收尾记录。

**后续开发（2026-09-10）：** 用户已选定并完成“输入框简洁易用、不要堆叠”。本地输入区改为消息框和单行工具栏，附件/字数按需显示，低频设置进入弹窗；详见 [输入区说明](COMPOSER.md)。生产构建、373 项逻辑与 13 组隔离 UI 检查通过，尚未提交或发布，版本仍为 0.1.12。保留原有交接文档改动；下一步可由用户查看效果并决定是否发行。以下为上一轮正式发布的交接基线。

更新：2026-09-10，会话收尾。**当前正式版本为 0.1.12，桌面发布与官网同步均已完成。** 用户决定在新会话做新功能，本次只整理文档，没有选择或开始下一项开发。旧文档中的 0.1.4/0.1.5 本地候选、“尚未发布”和旧待办属于历史记录；当前基线以本页和 [验证报告](VERIFICATION.md) 顶部为准。

## 1. 先核对当前工作区

| 项目 | 当前基线 |
| --- | --- |
| 桌面仓库 | C:/project/rivloom-opencode；rivloom/rivloom-desktop |
| 已发布桌面源码 | c039c7b8a3d43898d48ccedb469cbd79b68005ad，版本 0.1.12 |
| 官网仓库 | C:/project/rivloom-website；rivloom/rivloom-website；独立维护 |
| 已部署官网源码 | 7daee76d1d560fcaafad1eb44557e373a01ec9c9 |
| 正式构建 | v0.1.12-c039c7b8a3d4-10156362780 |
| 用户更新入口 | 关于 Rivloom → 检查更新；或[官网下载](https://rivloom.com/download/) |

开始时运行 git status 和 git log，保留现有工作区改动。本次收尾仅修改文档，未改产品代码、版本、锁文件或已发布安装包；不要把文档工作区与上述已发布二进制源码混为一谈。开发前先读本页，再按新功能阅读对应说明和源码，不必从旧路线图重新实现已有功能。

## 2. 已完成的功能

- 0.1.12：同一工作流会话的后续消息 FIFO 队列；Markdown 表格、代码高亮与复制；按用户/设备保存未发送草稿和已上传附件；截图粘贴、文件拖入与图片/文本/音视频预览。见[连续会话](CONVERSATION-CONTINUITY.md)。
- 0.1.10–0.1.11：目录/会话/文件右键菜单，目录别名、会话重命名与置顶，文件打开/定位/另存为/复制路径；紧凑消息、悬停复制图标、正文选择高亮。见[右键菜单](CONTEXT-MENUS.md)、[消息复制](MESSAGE-COPY.md)。
- 0.1.6–0.1.9：会话回收站、三个月保留和到期清理、恢复/永久删除；按目录分组与缩进；更新弹窗自动交接及备份进度；升级成功启动后清理旧升级备份。见[历史管理](CONVERSATION-HISTORY.md)、[应用内更新](DESKTOP-UPDATES.md)。
- 更早已完成：真实只读自动规划、依赖任务图、并行分支/汇合、资源查询、跨设备转交、审批与模型问题回答、结果优先/过程折叠、附件和成果回传、通知、诊断、中英文与可调侧栏。应用内更新、签名清单、Release/R2 下载和官网部署均已接通。

本机任务不设固定并发上限；外来任务使用独立名额，默认 3，可调 1–10。同一目录允许多个会话。应用身份为 Rivloom / desktop / com.rivloom.desktop。项目可以是普通文件夹，不要求 Git；执行仍使用官方 OpenCode，不 fork 或替换引擎。

## 3. 连续会话的关键约定

- 执行中追加的新要求进入同一会话队列。当前轮成功结束且全部执行确认结束后，按发送顺序自动开始下一轮；停止或失败暂停队列，等待明确继续。模型提出的问题在当前轮直接回答，不变成下一条排队消息。
- 会话 ID、偏好和历史入口保持稳定。已结束轮次保留完整要求、完成条件、回答、结果与文件；下一轮重新规划，通过完整 JSON 会话记录及成果继续。不是无限复用一个引擎 session。
- 最多 50 条待执行消息，每条 12,000 字符和 5 个附件。每步材料最多 10 个、合计 1000 MiB，会话记录文件也计入；超限明确暂停，不能静默丢弃上下文。
- 草稿只有收到发送成功确认后才清空；重复请求复用原 requestID。完成上传的附件可恢复；未完成上传须提示重新选择。永久删除与过期客户端重放不能复活已删除会话或草稿。
- 历史清理、通知去重、队列暂停/取消和更新阻塞都要覆盖归档轮次与待执行消息。旧普通 Task/Brain 记录继续沿用原规则，新会话统一走工作流。
- Markdown HTML 不执行，远程图片不自动加载；预览继续检查上传者/任务成员权限。文本上限 256 KiB，图片预览上限 20 MiB，媒体编码支持由 WebView 决定。

## 4. 下一批可选功能，尚未选定

以下是候选，不是本次授权一次性实现的待办；先听用户在新会话提出的具体目标。

| 候选 | 当前边界与最小方向 |
| --- | --- |
| 会话全文搜索 | 已有标题、要求、目录等筛选；尚无覆盖全部历史消息/结果正文的全文索引与命中跳转。需包括归档轮次，并遵守用户可见性、回收站和永久删除边界。 |
| 失败步骤单独重跑 | 已有暂停/继续/停止及规划重试；完整的业务步骤局部重试、下游结果失效和产物版本规则仍待设计。未知执行未停稳时不能重复运行。 |
| 任务模板、会话导出 | 现有复制和文件另存为不等于完整模板或会话导出。具体字段、导出格式和附件范围由用户选定。 |
| 结果自动校验 | 执行成功不等于任意产物符合业务要求。可对文件存在、格式、数量、时长等明确条件做客观核对；不恢复每次人工验收弹窗。 |
| 维护与恢复 | 已有回收站和成功升级后的旧备份清理，不能再次列为缺失。脱敏诊断包、有序数据迁移、用户恢复工具、Windows Authenticode 仍可单独规划。 |
| 首次离线投递 | 已创建投递有持久记录与重试，首次目标离线仍失败并保留草稿；持久未送达箱、取消/过期和重连一次投递可作为独立增强。 |

更多模型登录、macOS/Linux/ARM64、人员角色/计费、跨 Brain 协作和高可用仍需独立范围决定，不自动纳入本轮。

## 5. 已验证和仍待验证

0.1.12 最终提交三组 Windows CI、原生构建、隔离安装/启动/重启/卸载及发布全部通过。另行匿名完整下载 82,373,990 字节安装包，核对原更新公钥、签名清单、安装包签名/哈希、不可变版本与 latest 一致。官网 CI、Cloudflare 部署及正式域 23 项检查通过，覆盖 16 路由和 3 份静态资源。详细运行链接、哈希和证据见 [VERIFICATION](VERIFICATION.md) 顶部。

本地逻辑 373 项、官方引擎隔离工作流服务 18 项、历史服务 10 项检查通过。界面已核对 Markdown/高亮、即时语言切换、草稿恢复/发送、粘贴/选择器上传、图片/文本预览及窄屏。文件拖入、各类媒体编码的实际播放和新代码块的系统剪贴板全文未全部做真实 UI 验收，不夸大为全场景通过。

真实模型/双物理机的完整业务验收、真实断网与长时间负载仍是待验证事项，不直接等同已知软件缺陷。此前定位的软件问题已有针对性修复，但 Nemotron 的指令遵循/产物质量问题尚无完整跨模型对照，不能断言全部来自模型，也不能承诺任意模型输出质量。历史记录见[协作复测](plans/2026-09-09-collaboration-fix-retest.md)和[桌面验收](DESKTOP-ACCEPTANCE.md)。

用户安装版、真实项目/文件、既有备份、真实模型额度和暂停的物理机/性能测试不作为默认开发夹具；如新需求确实需要，由用户明确恢复对应范围。用独立合成数据验证，不修改原断言来掩盖失败。临时夹具与预览服务已关闭。

## 6. 按改动定位代码

| 范围 | 入口 |
| --- | --- |
| 工作流状态、轮次与队列 | shared/workflows.ts；server/workflows.ts；server/workflow-service.ts；server/workflow-api.ts |
| 规划/执行与材料上下文 | server/workflow-runtime.ts；server/workflow-files.ts |
| 会话输入、轮次和队列 UI | src/conversation-workspace.tsx；src/workflow-view.tsx |
| 草稿与 Markdown | src/conversation-drafts.ts；src/message-markdown.tsx；src/message-markdown.css |
| 附件预览 | shared/file-preview.ts；src/file-preview.tsx；server/task-files.ts |
| 历史及搜索 | shared/conversations.ts；shared/conversation-history.ts；src/conversation-filters.ts |
| 发布与升级 | docs/CI.md；docs/RELEASING.md；docs/DESKTOP-UPDATES.md；.github/workflows/windows-candidate.yml |

改动后运行相关测试、版本/测试分类/双语检查及构建；不要为文档收尾重跑安装或调用模型。新增 npm 依赖须同步锁文件和原始许可证清单，并通过 runtime 门禁。本轮曾漏记 Markdown 传递依赖声明，发行门禁发现后已完整修复。代码块组件订阅语言变化，避免 memo 缓存导致按钮保留旧语言。

发布新功能时使用后续版本，不能覆盖 0.1.12 或重置既有更新公钥/私钥。沿用目标提交全部 CI → 隔离安装 → Release → R2 → 签名更新 → 官网校验的现有链路。应用内更新签名已经具备，Windows Authenticode 发布者签名仍未具备，两者不要混淆。旧文档中“更新尚未上线”已过期。

本机操作补充放在被忽略的 .data/NEXT-SESSION-LOCAL.md；它只记录本地工具情况，不是可跨机器执行的发布脚本。旧 .data/publish-0.1.12-*.mjs 绑定已发布提交，不能直接用于后续版本。

## 给新会话的开场白

> 继续开发 Rivloom，工作区是 C:\project\rivloom-opencode。先读 docs/NEXT-SESSION.md、docs/HANDOFF.md 和 docs/VERIFICATION.md 顶部，再核对 Git 状态。当前 0.1.12 已发布，官网已同步；连续会话队列、Markdown/代码高亮、草稿保存、附件粘贴/拖入/预览均已完成，不要重做。保留现有改动，用隔离数据验证，暂不操作我的安装版、真实项目或真实模型。接下来我要做的新功能是：【填写具体功能和预期效果】。
