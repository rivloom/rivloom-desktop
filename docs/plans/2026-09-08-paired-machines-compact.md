# Compact paired machines

The user requests connected/offline groups (offline initially collapsed), compact two-line machine rows, execution-slot occupancy inside a progress bar with executing status, a small hardware-load indicator at the lower left, an information hover panel, and a local-remark pencil revealed by hovering the whole row. They confirmed the progress is occupied execution slots / total slots, not task completion.

Preserve the current history, queue actions, remark privacy, resizable sidebar and task routing. Use the existing design foundation and ui-ux-pro-max guidance. Keep connected rows around 64px, subdued surfaces, readable small text and no decorative animation. Make information accessible through hover, focus and touch, dismissible with Escape, and keep its panel inside the viewport even in a narrow or scrolled sidebar.

The existing worker runningTasks is actually occupied slots, including approval/review/reservations. Add counts-only optional workload telemetry to the existing authenticated queue report: occupiedSlots, totalSlots and executingCount. Negotiate node-workload-v1 and strip the new field for older peers. Do not change execution policy, admission, scheduling or share additional task titles. Legacy reports may provide occupancy but actual execution remains unknown. Expired/offline reports never imply zero load.

Hardware indicator = max of valid, fresh CPU / memory / GPU percentages; missing metrics are omitted and disclosed, never treated as zero. Details show the individual readings, hardware, sampling state, public task counts and only already-visible current conversations targeting that machine. Existing offline hardware may be shown as last reported.

1. Preserve the current production baseline and implement the scoped presentation / counts telemetry.
2. Verify validator boundaries, capability negotiation with a real prior implementation, stale/unknown/paused/over-capacity displays and task ownership. Run relevant existing tests and types/localization/production build.
3. Inspect isolated production UI in Chinese/English, normal/narrow/mobile sizes; verify group collapse, compact geometry, progress semantics, hardware calculation, hover/focus/touch/Escape, remark reveal, sidebar regressions and no unrelated calls.
4. Prepare a local installer from frozen source; verify extraction and packaging. Update this plan and handoff records with actual evidence. Do not execute the installer or publish it. Previously deferred physical/performance tests remain deferred.

## Verified implementation

The new `PairedMachines` component uses 63px desktop rows, ready-channel connected grouping, and native offline disclosure initially closed. Whole-row hover/focus reveals the existing local-only remark editor; touch devices keep that control visible. A single portaled information panel supports pointer transfer, keyboard focus/ArrowDown/Escape, outside dismissal and touch, with viewport-bounded scrolling. Only the list's one five-second display clock is added; it expires old statistics without new network polling. Incoming snapshots use current render time to avoid treating their timestamps as future values between ticks.

`node-workload-v1` adds three bounded integer counts to optional authenticated public queue telemetry. The configured slot count and occupied-slot calculation are unchanged; actual execution is counted only from `running` tasks. The wire serializer removes workload for prior queue-v1 peers, and ingress ignores unnegotiated workload. Existing legacy capacity is used only when accepting makes total slots inferable; execution remains unknown. No task titles, local remarks, paths or credentials are newly shared. Hardware bars show CPU/memory/GPU readings; their summary is the highest valid fresh reading. Missing and stale values stay unknown.

Types, production build, localization (809 UI/native keys) and diff whitespace checks passed. 54 focused logic tests plus 31 existing queue/resource/query tests passed. Three isolated authenticated compatibility tests passed, including the real previous `cad78d38a26cebc95909531870bf6b38cf58cac1` transport AND its strict statistics decoder, a current-to-current workload exchange, and the older pre-queue transport. The sandbox's Windows DPAPI restriction required rerunning these temporary-identity loopback tests with Windows key protection available; no installed identities or paired devices were used.

The existing 60 sidebar and 13 history/motion checks passed with all eight prior visible-history counts unchanged. Fifteen new production UI checks passed for grouping, 63px rows, truthful task capacity, hardware readings, remark interaction, full details and task ownership, hover/focus/Escape, one panel at a time, Chinese/English, narrow/mobile/touch, stale data and expiry without server updates. No browser errors, non-loopback requests or real task/model/control calls occurred. Final synthetic screenshots and reports: `.data/verification/paired-machines-20260908/verified-machines/`, `verified-sidebar/`, `verified-history/`. Initial UI-script failures were incorrect selectors/assumptions about the existing remark heading and mobile bottom rail, then an asynchronous timer assertion; these were corrected and the final checks passed. Existing Vite bundle-size advisory remains.

## Local installer

`.data/verification/paired-machines-candidate-20260908/delivery/Rivloom_0.1.4_machines_20260908_50011c3d_x64-setup.exe`, 79,861,733 bytes; SHA-256 `2286f7a166655d2cdbf73f05dda2ca1fce073c584206df2d704fe53ae694b747`. The frozen source digest `50011c3d2222b969b1e8758861ae26a05947b3880bac8a5c422bc3f3d0289504` covers 1,089 files on base `cad78d38a26cebc95909531870bf6b38cf58cac1`. Rust 1.98.1 Release compilation (1m43s) and NSIS packaging succeeded. Source and prepared runtime stayed unchanged, production dist equals packaged dist, all 7,643 extracted runtime files equal prepared bytes, and the x64 application differs from the native build only by the verified three-byte Tauri NSIS marker. Installer command comparison against the prior history-refinement candidate permits only asset filenames/estimated size; the other commands match. Evidence: `build-result.json`, `extraction-result.json`, `installer-static-review.json`, `ui-source-link.json`, and final `delivery-result.json` in the candidate directory.

Scope verification confirms 14 changed/new product/test/config files versus the previous candidate, limited to the paired-machine UI/helper, its locales and checks, the optional public workload protocol, its server provider and test registration. No existing source file was removed. Post-capture edits update only this plan and the three handoff documents. The local unsigned installer has not been executed or published; no commit/push was performed. Earlier installers remain available. Previously deferred installed/physical/performance checks remain deferred.
