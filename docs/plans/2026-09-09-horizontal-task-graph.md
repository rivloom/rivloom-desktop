# 任务图横向排列

任务依赖和执行转交改为从左到右展开。同一层的并行分支上下排列，前后步骤在相邻列居中连接；跨列依赖从卡片上方绕行。箭头方向与转交虚线一同调整。

卡片宽度按窗口和列数在 160–220 像素之间调整，高度为 104 像素。桌面约 800 像素宽的绘图区能完整展示常见的五步分支汇合图，绘图区高 260 像素；十步顺序流程保持一行，高 140 像素。长流程横向滚动，多并行分支的绘图区最高 300 像素，保留内部滚动和键盘访问。

4 项既有工作流界面逻辑检查及 7 项生产界面检查通过，覆盖分支汇合、十步顺序、转交、跨列依赖、点击与键盘选择、折叠、窄窗口和多并行分支；已查看桌面、转交和窄窗口截图。类型与构建通过。证据保存在 `.data/verification/horizontal-graph-20260909/`。

本轮仅改动任务图布局与样式，不改变步骤、依赖、执行记录或任务状态。新候选目录为 `.data/verification/horizontal-graph-candidate-20260909/`，包含此前文件链接、审批和停止队列修复，由用户自行安装。
